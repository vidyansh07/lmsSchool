# Feature status

The working task board. One row per feature. A row is `DONE` only when it is
tested and verified running — not when the code exists.

**Statuses:** `TODO` · `IN_PROGRESS` · `BLOCKED` · `DONE`

Totals at last full run (12 September 2026): **1,981 backend** (3 skipped) ·
**866 frontend unit** · **82 end-to-end** = 2,929 tests, all passing locally.

---

## Phase 0–3 — delivered

| Feature | Phase | Status | Main files | Tests | Security checks | Manual verification | Known limitation | Last verified |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Project foundation, environments, CI | 0 | DONE | `config/settings/*`, `.github/workflows/ci.yml` | `test_smoke`, `test_security_config` | `check --deploy` clean; env guards | Stack runs via compose | CI never executed on GitHub (no remote) | 2026-09-01 |
| Health probes | 0 | DONE | `apps/health/` | `test_health` | No config exposed | `/health/ready/` live | — | 2026-09-01 |
| Error envelope, audit foundation | 0 | DONE | `apps/common/exceptions.py`, `apps/audit/` | `test_errors`, `test_audit` | No traces leaked; secrets scrubbed | Live 4xx/5xx bodies | — | 2026-09-01 |
| Authentication (login/logout/reset/verify/sessions) | 1 | DONE | `apps/accounts/` | `test_auth`, `test_password_flows`, `test_email_verification` | Enumeration, CSRF, rate limits | Full flow via curl | No real SMTP credentials supplied | 2026-09-01 |
| Roles and capability matrix | 1 | DONE | `apps/accounts/roles.py`, `apps/common/permissions.py` | `test_security_access_control` | Fail-closed asserted | Per-role live checks | — | 2026-09-01 |
| User / student / trainer management | 1 | DONE | `apps/students/`, `apps/trainers/` | `test_user_admin`, `test_students_api`, `test_trainers_api` | IDOR, mass assignment | Live admin CRUD | — | 2026-09-01 |
| Profile image upload | 1 | DONE | `apps/common/uploads.py` | `test_profile_image_upload` | Content verified, EXIF stripped | PHP-as-PNG refused live | Local volume, not S3 | 2026-09-01 |
| Course catalogue and authoring | 2 | DONE | `apps/courses/` | `test_courses_api` | Draft invisibility | Live admin + trainer | — | 2026-09-01 |
| Modules, lessons, ordering, publishing | 2 | DONE | `apps/courses/services.py` | `test_courses_api` | Mass assignment | Live reorder | — | 2026-09-01 |
| Lesson resources | 2 | DONE | `apps/courses/`, `apps/common/uploads.py` | `test_course_resources` | Extension+magic pairing, traversal | Live upload/download | Local volume, not S3 | 2026-09-01 |
| Course access control | 2/3 | DONE | `apps/courses/access.py` | `test_courses_security`, `test_course_access_by_enrollment` | Unpublished invisible; enrolment gate | 200 enrolled / 403 not | — | 2026-09-01 |
| Batches and schedules | 3 | DONE | `apps/batches/` | `test_batches_api`, `test_schedule_conflicts` | Trainer scoping | Live CRUD | — | 2026-09-01 |
| Schedule conflict detection | 3 | DONE | `apps/batches/conflicts.py` | `test_schedule_conflicts` | Backend-enforced | Seeder proves refusal | — | 2026-09-01 |
| Enrolment | 3 | DONE | `apps/enrollments/` | `test_enrollments_api`, `test_enrollment_security` | Capacity race, duplicates, IDOR | Live §16 attack list | — | 2026-09-01 |
| Calendar | 3 | DONE | `apps/dashboards/calendar.py` | `test_dashboards_calendar` | Per-source access control | Live student calendar | — | 2026-09-01 |
| Student + trainer dashboards | 3 | DONE | `apps/dashboards/views.py` | `test_dashboards_calendar` | Query ceilings asserted | Live both roles | — | 2026-09-01 |
| Lesson progress foundation | 3 | DONE | `apps/enrollments/models.py` | `test_dashboards_calendar` | Owner-only | Live completion toggle | Minimal by design | 2026-09-01 |

---

## Phase 4 — academic operations

| Feature | Phase | Status | Main files | Tests | Security checks | Manual verification | Known limitation | Last verified |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 4.0 SUPERADMIN + MANAGER roles | 4 | DONE | `apps/accounts/roles.py` | `test_security_access_control` (8 new) | Ladder monotonic; platform config superadmin-only; manager cannot change accounts | Manager and superadmin reach the right endpoints | — | 2026-09-01 |
| 4.1 Class sessions | 4 | DONE | `apps/sessions/` (models, services, access, views) | `test_class_sessions` (23) | Trainer cannot touch an unrelated batch's class; students read-only | Generation idempotent; reschedule keeps the original | Frontend not yet built | 2026-09-01 |
| 4.1a Trainer assignment history | 4 | DONE | `apps/sessions/models.py`, `apps/batches/services.py` | `test_class_sessions` | Students cannot read staffing history | Opening stint recorded at batch creation | — | 2026-09-01 |
| 4.2 Daily attendance | 4 | DONE | `apps/attendance/` | `test_attendance` (21) | Trainer scope, student isolation, no marker identity leaked | Bulk marking, corrections, percentages | Frontend not yet built; no admin on/off switch yet (4.7) | 2026-09-01 |
| 4.3 Assignment system | 4 | DONE | `apps/assignments/` (models, access, services, views) | `test_assignments` (36) | Trainer scope, student isolation, server-side mark bound, strict fields | Create, publish, submit, grade, return, resubmit | Frontend not yet built; per-assignment passing mark until 4.7 | 2026-09-01 |
| 4.4 Assignment file security | 4 | DONE | `apps/common/uploads.py`, `assignments/views.py` | `test_assignment_files` (31) | Allowlist, magic-byte pairing, inert `.bin` storage, octet-stream attachment, nosniff, sandbox CSP, download authorization | Code, PDF, forged PDF, executables, traversal names, classmate download | Local filesystem storage; S3 not wired (no credentials) | 2026-09-01 |
| 4.5 Weekly tests / external assessments | 4 | DONE | `apps/assessments/` (models, access, services, views) | `test_assessments` (23) | Trainer scope, student isolation, https-only links, server-side mark bound | Schedule, publish, mark, absent, file-backed test graded through the assignment path | Native quiz engine is Phase 5 | 2026-09-01 |
| 4.6 Trainer result import | 4 | DONE | `apps/assessments/importers.py`, `views.ResultImport*` | `test_result_import` (26) | Type/size/row bounds, formula-cell refusal, unknown-student and duplicate detection, all-or-nothing transaction, import audit record | CSV and XLSX preview then confirm; cohort change between the two rolls back | Import runs inline, not queued (no worker yet) | 2026-09-01 |
| 4.7 Academic configuration | 4 | DONE | `apps/academics/` (models, policies, services, views) | `test_academic_config` (20) | `academic.configure` is admin-only; reading is open; unknown rules rejected | Changing the passing % flips a stored mark's verdict with no deployment | Per-course overrides only, not per-batch (D-032) | 2026-09-01 |
| 4.8 Phase 4 frontend | 4 | DONE | `frontend/app/teaching/*`, `app/my-*`, `app/admin/academics` | `playwright e2e/academics.spec.ts` (5) | UI hides what a role cannot use; every control re-checked by the backend | All ten screens loaded in a browser against the seeded stack | No offline/PWA support | 2026-09-01 |
| 4.8 Phase 4 end-to-end journeys | 4 | DONE | `frontend/e2e/academics.spec.ts` | 3 named journeys + 2 refusal cases | Executable upload refused in the browser; unknown student blocks an import | Register → student sees it; set → submit → grade → student sees grade; import preview → confirm → student sees result | — | 2026-09-01 |
| 4.x Role-grant containment (security fix) | 4 | DONE | `apps/accounts/roles.py::can_grant_role` | `test_security_access_control` (6 new) | An admin cannot mint a superadmin; refusals are audited | Verified through the users API | — | 2026-09-01 |
| 4.x Academic demo seed | 4 | DONE | `apps/assessments/management/commands/seed_academics.py` | `test_seed_academics` (10) | Refuses to run where demo data is not allowed | `make seed-academics`; converges on repeat runs | — | 2026-09-01 |
| 5.1 Project system | 5 | DONE | `apps/projects/` (models, access, services, views) | `test_projects` (38) | Trainer scope, student isolation, server-summed rubric, transition table | Set, publish, assign, hand in, rework, approve | Team projects not built (field reserved, D-034) | 2026-09-01 |
| 5.2 Multiple project rules | 5 | DONE | `services.required_project_progress` | `test_projects` | Students read only their own progress | Required vs optional; outstanding list surfaced | Read by Phase 6 completion | 2026-09-01 |
| 5.3 Project file security | 5 | DONE | `apps/projects/models.py`, `views.ProjectFileView` | `test_projects` (4 file tests) | Same pipeline as §4.4: allowlist, magic bytes, inert `.bin`, octet-stream attachment, sandbox CSP | Executable refused; traversal name neutralised; classmate 404 | Local filesystem storage; S3 not wired | 2026-09-01 |
| 5.4 Native question bank | 5 | DONE | `apps/questions/` | `test_question_bank` (19) | Students get an empty queryset — the bank holds the answers; broken option sets refused | MCQ, multiple, true/false, short, long, file | Bulk import not built | 2026-09-01 |
| 5.5 Final examination engine | 5 | DONE | `apps/exams/` | `test_exams` (35) | Frozen paper, server clock, server-computed score, system RNG, no answer fields in the candidate serializer | Draw, auto-save, submit, auto+manual marking, release | Expiry is lazy, not swept (D-038) | 2026-09-01 |
| 5.6 Exam failure handling | 5 | DONE | `apps/exams/services.py` | `test_exams` (11 failure cases) | Refresh, browser close, duplicate submit, expiry, timer tampering, attempt limit, unauthorized access, cross-student access, concurrent start | All nine reproduced as tests | — | 2026-09-01 |
| 5.7 Phase 5 frontend | 5 | DONE | `frontend/app/teaching/{projects,exams,questions}`, `app/my-projects`, `app/exams`, `app/attempts` | `playwright e2e/projects-exams.spec.ts` (4) | Review controls only on handed-in work; candidate never sees an answer | All seven screens driven in a browser | — | 2026-09-01 |
| 5.7 Phase 5 end-to-end journeys | 5 | DONE | `frontend/e2e/projects-exams.spec.ts` | 2 named journeys + 2 refusal cases | Executable deliverable refused; another candidate's attempt 404s | Set → hand in → review → feedback; create exam → sit → auto-save → submit → release → result | Suite is repeatable: runs back to back green | 2026-09-01 |
| 5.x Concurrent exam start (bug fix) | 5 | DONE | `apps/exams/services.py::start_attempt` | `test_exams` threaded test | Two simultaneous starts return one attempt | Found by the E2E run, not by review | — | 2026-09-01 |
| 5.x Local Redis cache | 5 | DONE | `docker-compose.yml`, `requirements/base.txt`, `Makefile` | `test_health` | Rate-limit counters now shared across workers | Cache roundtrip verified in Redis; readiness `ok` | Not yet a Celery broker | 2026-09-01 |
| 6.1 Lesson progress | 6 | DONE | `apps/enrollments/models.py`, `apps/progress/reports.py` | `test_progress_completion` | Enrolment isolation | Draft lessons excluded; counts match modules | Video resume deliberately not built (§6.1) | 2026-09-01 |
| 6.2 Module progress | 6 | DONE | `apps/progress/reports.py::module_progress` | `test_progress_completion` | — | One grouped query, not one per module | — | 2026-09-01 |
| 6.3 One course progress calculation | 6 | DONE | `apps/progress/reports.py::progress_report` | `test_progress_completion` (35) | Students read only their own | Every screen reads it; none computes its own | — | 2026-09-01 |
| 6.4 Configurable completion rules | 6 | DONE | `apps/progress/rules.py`, `apps/academics/models.py` | `test_progress_completion` | Rules are admin-configurable only | All seven conditions; off-rules still reported; threshold change takes effect at once | — | 2026-09-01 |
| 6.5 Online + offline students | 6 | DONE | `apps/batches/models.py::DeliveryMode`, `Enrollment.effective_delivery_mode` | `test_progress_completion` | — | One architecture; per-student override | Nothing branches on mode beyond configuration | 2026-09-01 |
| 6.6 Completion workflow | 6 | DONE | `apps/progress/` (models, services, views) | `test_progress_completion` | `completion.approve` is admin-only; trainers and students refused | Eligible → approve → enrolment completes; override recorded as one | — | 2026-09-01 |
| 6.7 Certificates | 6 | DONE | `apps/certificates/` | `test_certificates` (28) | Approval required before issue; snapshot frozen; one live certificate per completion | Issue, reissue, revoke, PDF with QR | PDFs rendered on demand, not stored | 2026-09-01 |
| 6.8 Public verification | 6 | DONE | `certificates/views.py::VerifyCertificateView` | `test_certificates` | Unguessable 160-bit code, throttled, allowlist response, revoked still verifies | Checked signed-out in a browser | — | 2026-09-01 |
| 6.9 Phase 6 frontend and E2E | 6 | DONE | `frontend/app/my-progress`, `app/admin/{completions,certificates}`, `app/verify/[code]` | `playwright e2e/completion-certificates.spec.ts` (5) | Public page leaks no email, student id or batch code | Approve → issue → download → verify, then revoked as teardown | — | 2026-09-01 |
| 6.x E2E repeatability | 6 | DONE | `frontend/e2e/*.spec.ts` | full suite twice, back to back | — | Journeys derive their batch from the student's own enrolments and restore what they consume | — | 2026-09-01 |
| 7.1 Notifications | 7 | DONE | `apps/notifications/` | `test_notifications` (25) | Scoped to the recipient; a deactivated account is never notified | Eight event kinds wired to real events | In-app and email only; no push (D-049) | 2026-09-01 |
| 7.2 Email service layer | 7 | DONE | `notifications/channels.py`, `templates.py`, `send_pending_email` | `test_notifications` | Failures scrubbed of secrets before storing or logging | Send, fail, retry, give up | Retry is a cron command, not a worker (D-052) | 2026-09-01 |
| 7.3 Announcements | 7 | DONE | `apps/announcements/` | `test_notifications` | Trainers limited to their own batches; students never see a draft | Draft → publish → cohort notified | — | 2026-09-01 |
| 7.4 Unified calendar | 7 | DONE | `apps/dashboards/calendar.py` (4 new sources) | `test_learning_and_discussions` | Each source resolves its own access | Classes, assignments, tests, exams, projects in one feed | — | 2026-09-01 |
| 7.5 Student learning experience | 7 | DONE | `apps/learning/` | `test_learning_and_discussions` (22) | Bookmarks and notes are private; no staff view | Continue learning, recent, history, upcoming | — | 2026-09-01 |
| 7.6 Discussions | 7 | DONE | `apps/discussions/` | `test_learning_and_discussions` | Batch-scoped; moderation is trainer-or-capability; hidden replies stay visible to their author | Ask, answer, pin, close, hide | — | 2026-09-01 |
| 7.7 Batch directory | 7 | DONE | `learning/views.py::BatchDirectoryView` | `test_learning_and_discussions` | Names and student ids only; switchable off | Verified no contact data reaches the page | — | 2026-09-01 |
| 7.8 Gamification | 7 | NOT BUILT | — | — | — | — | Deliberate: bookmarks and notes built, badges and points declined (D-057) | 2026-09-01 |
| 7.9 Phase 7 frontend and E2E | 7 | DONE | `frontend/app/{notifications,announcements,discussions,my-learning}` | `playwright e2e/communication.spec.ts` (7) | Draft invisible to students; no email address on any student-facing page | Announce → notified; ask → answered → closed | — | 2026-09-01 |
| 7.x Project list N+1 (bug fix) | 7 | DONE | `projects/views.py`, `services.ensure_student_projects` | `test_projects` query-count test | — | The page made one request per project and got slower every run | — | 2026-09-01 |
| 8.1 Configuration centre | 8 | DONE | `apps/academics/` (grade bands, academic calendar) | `test_reporting`, `test_academic_config` | `academic.configure` only; bands validated | Bands change grades in reports at once; holidays skip class generation | Email templates stay in code (D-051) | 2026-09-01 |
| 8.2 Trainer configuration | 8 | DONE | `apps/batches/`, `apps/courses/` (existing), `reports/trainer_activity` | `test_reporting` | Trainers stay scoped to their batches everywhere | Assignment, reassignment and authoring already existed; activity now reportable | — | 2026-09-01 |
| 8.3 Reports | 8 | DONE | `apps/reporting/reports.py` (10 reports) | `test_reporting` (60) | Built from a scoped queryset; a filter cannot widen access | Every report run and exported against real rows | — | 2026-09-01 |
| 8.4 Dashboards | 8 | DONE | `apps/reporting/dashboards.py` | `test_reporting` query-count tests | Admin dashboard is capability-gated | Bounded queries; batch summaries in one grouped query | — | 2026-09-01 |
| 8.5 Bulk import/export | 8 | DONE | `apps/reporting/importers.py`, `exports.py` | `test_reporting` | Preview→confirm, all-or-nothing, formula neutralisation, separate export capability | Students and attendance; CSV streamed | XLSX export not built — CSV only | 2026-09-01 |
| 8.6 Analytics | 8 | DONE | `apps/reporting/metrics.py` | `test_reporting` | Scope enforced inside the metric helper | Nine metrics, each with a documented definition; attendance trend by week | — | 2026-09-01 |
| 8.7 Phase 8 frontend and E2E | 8 | DONE | `frontend/app/admin/{overview,reports,imports}` | `playwright e2e/reporting.spec.ts` (7) | Student sees no reports; trainer reads but cannot export | Overview, run report, export CSV, import preview → confirm | — | 2026-09-01 |
| 8.x Readiness probe race (bug fix) | 8 | DONE | `apps/health/checks.py` | `test_health` threaded test | Concurrent probes no longer fail each other | Found by a full E2E run reporting a false 503 | — | 2026-09-01 |
| 9.1 Authentication hardening | 9 | DONE | `apps/accounts/` (unchanged), `tests/test_auth.py` | `test_auth` (17) | Throttled, generic errors, hashed tokens, secure cookies | Sign-out-everywhere ends a second browser; an expired session is refused | No MFA (documented) | 2026-09-01 |
| 9.2 Authorization matrix | 9 | DONE | `tests/test_authorization_matrix.py` | 160 tests | Every route swept per role; object-level cross-tenant checks | A student typing an admin URL gets nothing back | — | 2026-09-01 |
| 9.3 Private file storage | 9 | DONE | `apps/common/storage.py`, `config/settings/` | `test_file_storage_security` (21) | Private ACL, signed expiring URLs, boot guard against a public bucket | Real round trip against a mocked S3 bucket | No bucket provisioned — credentials are a human step | 2026-09-01 |
| 9.3a Malware scan hook | 9 | DONE | `apps/common/scanning.py` | `test_file_storage_security` | Fails closed; refusal never names the detection; audited | Rejection path exercised by a test scanner | No scanner configured | 2026-09-01 |
| 9.4 API security | 9 | DONE | `apps/common/throttling.py`, `apps/reporting/views.py` | `test_data_and_audit_security`, `e2e/hardening.spec.ts` | Exports and imports carry their own rate limit; headers verified in the browser | Headers and cookie flags checked against the running stack | — | 2026-09-01 |
| 9.5 Data minimisation | 9 | DONE | serializers (unchanged), `tests/test_data_and_audit_security.py` | 31 tests | Rosters, registers and the directory carry no contact or fee data | Asserted per field on live responses | — | 2026-09-01 |
| 9.6 Audit completeness | 9 | DONE | `apps/common/exceptions.py`, `apps/audit/` | `test_data_and_audit_security` | Every 403 recorded, deduplicated against view-level entries | Every category in §14.6 asserted present | — | 2026-09-01 |
| 9.7 Dependency security | 9 | DONE | `.github/workflows/ci.yml`, `docs/security.md` §16 | CI gates | pip-audit, npm audit, bandit, gitleaks all clean | Run locally and in CI | CI has never executed — no git remote | 2026-09-01 |
| 9.8 Database security | 9 | DONE | `infra/db/least-privilege.sql`, `tests/test_database_security.py` | 21 tests | Two roles, no DDL for the app, append-only audit log | Grants applied to a scratch database and verified | Roles are a deployment step | 2026-09-01 |
| 9.9 Performance at scale | 9 | DONE | `apps/progress/bulk.py`, `apps/reporting/`, `seed_scale_data` | `tests/test_performance.py` (27) | Reports stay scoped while going flat | 400 students / 16k attendance / 3.2k results; every report profiled | Scale data needs its own database | 2026-09-01 |
| 9.9a N+1 and slow-query fixes | 9 | DONE | reports, dashboards, `Enrollment.with_related` | `test_performance` growth tests | — | student_progress 7,400→42 queries; batch dashboard 4,097→18 ms; enrolment list 34→9 queries | — | 2026-09-01 |
| 9.10 Background work | 9 | DONE | `config/celery.py`, `apps/notifications/tasks.py`, compose worker+beat | `tests/test_background_work.py` (14) | No result backend; eager mode refused in deployment; credential mail never queued | Real worker consumed real tasks; beat fired the sweep on schedule | Redis is local — no managed broker | 2026-09-01 |
| 9.11 Integration boundaries | 9 | DONE | `apps/assessments/models.py`, settings | `test_data_and_audit_security` | External tests are an https link only; no gateway; credentials from env | Asserted no token/credential field exists | — | 2026-09-01 |
| 9.x Deterministic pagination (bug fix) | 9 | DONE | `apps/common/pagination.py` | `test_performance` paging tests | A row can no longer appear on two pages or none | Paged through tied rows and through a nullable sort column | — | 2026-09-01 |
| 9.x Archive a brief (UI gap) | 9 | DONE | `app/teaching/{assignments,projects}/[id]/page.tsx` | `e2e/academics.spec.ts`, `e2e/projects-exams.spec.ts` | Same capability as publish/close | Trainer archives; the student's list drops it; handed-in work is kept | — | 2026-09-01 |
| 10.1 Final regression | 10 | DONE | whole suite | 1,221 + 61 + 69 | Every gate clean | Suite green on the dev stack; journey green on clean staging | 7 specs fail on staging (see RELEASE_READINESS) | 2026-09-02 |
| 10.2 Mandatory journey | 10 | DONE | `frontend/e2e/release-journey.spec.ts` | 1 journey, 23 steps | Ends signed out, verifying publicly | Repeated passes on a rebuilt-from-empty staging | Learns as a seeded student (D-086) | 2026-09-02 |
| 10.3 Verification script | 10 | DONE | `scripts/verify_demo.sh` | 19 checks | Refuses an environment holding a non-reserved address | Run against local and staging, both clean | — | 2026-09-02 |
| 10.4 Staging environment | 10 | DONE | `docker-compose.staging.yml`, `infra/proxy/` | `verify_demo.sh staging` | DEBUG off, hardened settings, TLS, secure cookies | Full stack up, seeded, E2E run against it | Self-signed certificate; MinIO not AWS | 2026-09-02 |
| 10.5 Staging seed dataset | 10 | DONE | `seed_demo_data`, `seed_academics` | `test_seed_*` (16) | Fake data only, reserved domains | Every §15.5 item present, with edge cases | — | 2026-09-02 |
| 10.6 Migration safety | 10 | DONE | `scripts/check_migrations.sh`, `docs/operations.md` | 4 checks | No destructive operation in 58 migrations | Fresh install, reverse and re-apply, all clean | — | 2026-09-02 |
| 10.7 Backup and restore | 10 | DONE | `scripts/backup.sh`, `docs/operations.md` | `--verify` | Backups are git-ignored | Dump restored into a scratch database, sequences checked | Object storage not backed up — no bucket | 2026-09-02 |
| 10.8 Production configuration | 10 | DONE | `.env.production.example`, `infra/docker/` | `check --deploy` | gunicorn, never runserver; no secret in the template | Both deploy checks clean | Never deployed | 2026-09-02 |
| 10.9 Final security gates | 10 | DONE | `make security`, `make secrets` | all | bandit, pip-audit, npm audit, gitleaks, deploy checks | All clean; the ignore guard proven to fail | CI has never run | 2026-09-02 |
| 10.10 Release report | 10 | DONE | `docs/RELEASE_READINESS.md` | — | States plainly it is not production ready | Five human tasks listed | — | 2026-09-02 |
| 10.x CSP blocked hydration (bug fix) | 10 | DONE | `frontend/middleware.ts`, `app/layout.tsx` | `verify_demo.sh` nonce check, E2E on staging | Nonce per request, no `unsafe-inline` | The production build works in a browser | — | 2026-09-02 |
| 10.x Stale CSRF after sign-out (bug fix) | 10 | DONE | `frontend/lib/api.ts` | `tests/unit/api.test.ts` (2) | Only `csrf_failed` retried, once | Sign out, sign in, act — no longer fails first | — | 2026-09-02 |
| 10.x Generate classes (UI gap) | 10 | DONE | `app/admin/batches/[batchId]/page.tsx` | release journey | Same capability as the API | A timetable becomes classes from the screen | — | 2026-09-02 |
| 10.x Mark a lesson complete (UI gap) | 10 | DONE | `app/courses/[slug]/learn/[lessonId]/page.tsx` | release journey | Students only, via their enrolment | A student can finish a lesson | — | 2026-09-02 |
| 11.1 Role hierarchy | 11 | DONE | `apps/accounts/roles.py`, `services.py` | `test_role_hierarchy` (50) | Authority downward only; refusals audited with both roles | Every ordered pair of roles checked | Superadmins may administer each other, by design (D-096) | 2026-09-02 |
| 11.2 User administration | 11 | DONE | `app/admin/users/[userId]/`, `user_views.py` | `test_role_hierarchy`, `e2e/role-administration.spec.ts` (6) | Email change unverifies and ends sessions; links not passwords | Superadmin configures an admin end to end in a browser | Verification cannot be granted by hand (D-100) | 2026-09-02 |
| 11.3 Superadmin holds every power | 11 | DONE | `roles.py`, route sweep | `test_authorization_matrix` | No route refuses a superadmin | Whole resolver swept | Two self-service `/me/` routes excepted, named | 2026-09-02 |
| 11.4 Light theme | 11 | DONE | `app/globals.css` | `tests/unit/theme-contrast.test.ts` (14) | Every pair meets WCAG AA, measured not estimated | Dark-OS viewer still sees light | No dark mode, by request | 2026-09-02 |
| 11.5 Sidebar navigation | 11 | DONE | `components/app-shell.tsx`, `navigation.ts` | `e2e/interface.spec.ts` (7) | Links filtered by capability; server still enforces | Keyboard reachable, focus visible, closes on navigation | Students keep the top bar, by design | 2026-09-02 |
| 11.6 Interface quality | 11 | DONE | shell, cards, fields | `interface.spec.ts` | — | One current-page marker; grouped navigation; stale footer removed | — | 2026-09-02 |
| 11.x Deferred audits leaked between tests (bug fix) | 11 | DONE | `tests/conftest.py` | whole suite | — | A queued entry no longer surfaces in a later test | — | 2026-09-02 |
| 10.x Cancelled batch on today's list (bug fix) | 10 | DONE | `apps/sessions/views.py` | `test_class_sessions` (2) | A cancelled batch is not teaching today | Its class disappears; history kept | — | 2026-09-02 |
| 10.x Seeder died on a batch with no timetable (bug fix) | 10 | DONE | `seed_academics.py` | `test_seed_academics` | Skipped and reported, not fatal | Seed completes with timetables stripped | — | 2026-09-02 |
| 10.x One shared E2E sign-in helper | 10 | DONE | `frontend/e2e/helpers.ts` | whole suite | Waits out the credential throttle rather than raising it | 45→61 staging passes | Seven specs still dev-specific | 2026-09-02 |
| 12.13 Centres (branch scoping) | 12 | DONE | `apps/organisation/`, sixteen `access.py` modules, `frontend/app/admin/branches`, `components/organisation/branch-field.tsx` | `test_branch_scoping` (248), `test_branch_scoping_api` (218), `organisation/tests/test_scoping` (32) | A null branch is `.none()`, never the institution; every id from another centre is a 404 | Two populated centres; the noticeboard and bulk-import leaks each reproduced over HTTP before being fixed | Courses and the question bank stay institution-wide, by design (D-129). Merged 2026-09-14 on top of Phase 13: the fees API, the SITP importer and every create form now carry the centre; a superadmin is asked which centre on create, everybody else's own is forced | 2026-09-14 |
| 12.14 Institution settings | 12 | DONE | `apps/configuration/` (one row, named columns), `frontend/app/admin/settings`, `components/support-card.tsx` | `test_system_settings`, `admin-settings-page`, `support-card`, `e2e/settings.spec` | `settings.manage` for reads and writes by staff, public subset on `settings/public/`; every setting has a consumer | Institution name and support contact on mail and the profile screen; email channel on/off; export retention; upload ceiling | Not per-centre, by design; no cache | 2026-09-14 |
| 12.15 Manager and counsellor as equals; a manager teaches | 12 | DONE | `apps/accounts/roles.py`, `apps/organisation/scoping.py`, `apps/trainers/` (`ensure_teaching_profile`, `POST trainers/teaching-profile/`), `apps/dsr/services.py`, `Course.default_fee` | `test_roles_equal_and_manager_teaches` (7), `test_counsellor_rbac` (rewritten), `test_branch_scoping*` (rewritten for an unbounded admin) | Ladder still monotonic (`test_role_hierarchy`); a counsellor cannot make anybody a trainer; a student cannot be made one by a search box; a trainer still cannot review their own report | Counsellor marks a register and opens a course shell with a usual fee; a manager picked as trainer gets a profile, is assigned, reaches `/trainers/me/`, signs off their own DSR; an administrator sees both centres | D-130, D-129 amended. Manager and counsellor sidebars unchanged by request; capability-gated entries appear for both | 2026-09-14 |
| 12.16 Activity review and the ranked administrator sidebar | 12 | DONE | `apps/activity/` (feed and scorecards over the audit log), `frontend/app/admin/activity`, `components/navigation.ts` (`ADMIN_NAV`, `navFor`) | `test_activity` (8), `activity-page` (2), `app-shell` | `audit.view` only; noise actions (sign-ins, listings, downloads, refusals) never shown or counted; "changes only" by decision | Scorecards per person for today/week/month/custom, busiest first; feed as sentences with links; click a person or a kind to narrow | Views are not logged (10b), so not counted; export of the range arrives with the exports work | 2026-09-14 |
| 12.17 Warnings | 12 | DONE | `apps/warnings/` (services, endpoint, weekly digest task), `frontend/components/warnings-strip.tsx` on the admin overview, batch review, admissions dashboard and trainer dashboard; "No trainer" badges inline | `test_warnings` (6), `warnings-strip` (2) | Computed over the caller's visible querysets only; a student gets nothing, a trainer their own classes, a manager their centre; cached 60 s per person | Eight kinds the owner asked for plus accounts without a centre, unverified staff email, pending completions and overrun batches; Monday 08:00 digest to every staff inbox (email when preferences allow) | Trainer clash check is capped at 300 open batches | 2026-09-14 |
| 12.18 Everything exportable | 12 | DONE | `apps/reporting/reports.py` (students, enrolments, fee payments, daily reports, batches, activity record as reports), `views.py` (`?as=xlsx|pdf` inline up to 2,000 rows, 409 past it), `tasks.py` (a notification when a queued export is ready or fails), `apps/fees/receipts.py` (receipt PDF per payment); `frontend/components/export-menu.tsx` on every list, `export-jobs-panel.tsx` on Reports | `test_list_exports` (13), `test_reporting` (every report in every format), `export-menu` (3), `export-jobs-panel` (1) | Same scoped querysets as the screens; `student`, `since/until`, `actor`, `kind`, `role` narrow and never widen; the activity export needs `audit.view`; a receipt is for staff who see the enrolment or the student it belongs to | Excel, PDF and CSV from students, admissions, batches, trainers, daily reports, the activity review, a student's ledger and the reports screen; a receipt link on every payment; "Your exports" with download and cancel | PDF is capped at 2,000 rows whichever route | 2026-09-14 |
| 12.19 Hardening: soft delete for content, caching, timeouts, backups | 12 | DONE | `apps/common/caching.py`; `SoftDeleteBaseModel` on Module, Lesson, LessonResource, Assignment, Project, Question, Assessment (+ migrations); `apps/common/recovery.py` (`CASCADES`, centre paths); `config/settings/base.py` (statement timeout); `infra/scripts/backend-entrypoint.sh` (30 s); `frontend/lib/api.ts` (20 s); `scripts/install-backup-timer.sh` | `test_content_soft_delete` (6), `test_caching` (4), plus every existing delete test | Deleted rows leave every normal query and are restorable by an administrator within their centre; caches are per person and forgotten on write; nothing purges on a schedule | Deleted a lesson: gone from the tree and the player, listed in the bin, restored with its resources; dashboards served from cache for a minute; nightly dump and Sunday restore check on the host | Tests run in eager mode; the Redis-backed cache is exercised on staging only | 2026-09-14 |
| 12.20 Trainer requirements and announcements to the trainers | 12 | DONE | `apps/requirements/` (model, access, services, API), `apps/announcements` (`Audience.TRAINERS`), `apps/activity/services.py` (feed lines), `frontend/components/requirements/requirements-board.tsx`, `app/requirements/page.tsx`, sidebar entries for managers, trainers and administrators | `test_requirements` (11), `requirements-board` (5) | `requirement.manage` (manager and up; not the counsellor) raises, closes and removes; a trainer reads and answers on their own centre's; a student is refused; another centre's is a 404 | A manager raises "cover Linux next week": every trainer of the centre is notified, a trainer answers, the manager marks it fulfilled naming them, and the activity review shows all three steps | Replies are not editable; no attachments | 2026-09-14 |

## Phase 12 — ERP foundation

Built on the audited baseline in `PROJECT_IMPLEMENTATION_REPORT.md`. The eight
gaps that report names are being closed in order; this table grows a row per
phase.

| Feature | Phase | Status | Main files | Tests | Security checks | Manual verification | Known limitation | Last verified |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 12.1 COUNSELLOR role | 12 | DONE | `apps/accounts/roles.py`, `migrations/0004_alter_user_role.py` | `test_counsellor_rbac` (63), `test_role_hierarchy` (62; 36 authority pairs + a completeness guard), `test_authorization_matrix` | Ladder containment asserted; the role holds nothing academic and no `user.*`; both authority gates tested together | Registered a student, opened and staffed a batch, enrolled — all through the API as a counsellor | No counsellor screens yet; the workflow UI is Phase 5 of the plan | 2026-09-06 |
| 12.2 DSR, performance and export capabilities | 12 | DONE | `apps/accounts/roles.py` | `test_counsellor_rbac`, `test_authorization_matrix` | Declared before use, so the phases that consume them cannot invent their own check | Present in the capability matrix and the generated schema | Nothing consumes them yet — deliberate, they land with their features | 2026-09-06 |
| 12.3 Report export requires read access | 12 | DONE | `apps/reporting/views.py` | `test_counsellor_rbac` (3 export cases) | A file is no longer a way to read a report the screen refuses | Counsellor 403, manager 200 with a CSV, trainer 403 | — | 2026-09-06 |
| 12.4 Capability mirror enforced | 12 | DONE | `frontend/tests/unit/capability-mirror.test.ts` | 5 | A typo'd capability name can no longer silently hide a control forever | Verified it fails when a constant is renamed | Parses the backend source; a large refactor of `roles.py` would need the parser revisited | 2026-09-06 |

| 12.5 DSR | 12 | DONE | `apps/dsr/` (models, access, services, serializers, views) | `test_dsr` (68) | Trainer scope, cannot review own report, counsellor and student refused | Six-state workflow driven end to end; counts prefilled from the register | Frontend capture screen is 12.10 | 2026-09-07 |
| 12.6 Course timeline | 12 | DONE | `apps/sessions/`, `apps/progress/reports.py` | `test_course_timeline` | Trainer scoped to own batches | Autoplan assigns in curriculum order and is idempotent | Ahead/behind thresholds are module constants; belong in `AcademicPolicy` per course | 2026-09-07 |
| 12.7 Performance and risk engine | 12 | DONE | `apps/performance/` | `test_performance_engine` (95) | Student sees only their own; nobody reviews themselves | Thresholds change a verdict with no deployment | Risk flags are computed live, never stored — so they cannot be acknowledged | 2026-09-07 |
| 12.8 Export jobs | 12 | DONE | `apps/reporting/` (models, writers, tasks) | `test_export_jobs` | Scope re-derived inside the task, never carried in the payload; formula neutralisation in XLSX | CSV, XLSX and PDF written to private storage | PDF capped at a documented row limit | 2026-09-07 |
| 12.9 Transfers, upgrades, batch kinds | 12 | DONE | `apps/batches/`, `apps/enrollments/` | `test_batch_transfer` (20) | A transfer cannot get a student into a full batch | Attendance stays honest across a move via `transfer_chain` | `MODULAR` is stored; nothing branches on it pending a client decision | 2026-09-07 |
| 12.10 Role screens | 12 | DONE | `frontend/app/{manage,admissions,teaching/today,dashboard}` | 581 frontend unit tests | Every page gated by capability; backend re-checks | Manager hubs, counsellor pipeline, trainer end-of-class capture, student dashboard | Inline risk-acknowledge and review-done need backing models first | 2026-09-07 |
| 12.11 Recycle bin | 12 | DONE | `apps/common/recovery.py`, `frontend/app/admin/recovery` | `test_recovery` (27) | Model label matched against a closed registry, never `get_model` | Restore and purge driven through the API and the admin screen | — | 2026-09-07 |
| 12.12 Set a batch up for teaching in one action | 12 | DONE | `apps/batches/services.py::setup_for_teaching`, `frontend/app/admin/batches/[batchId]` | `test_batch_setup` | `batch.manage_schedule`; refuses a trainer clash | Six days a week by default; classes generated and lessons placed in order | — | 2026-09-07 |

---

## Phase 13 — client delivery (8–12 September 2026)

What the owner asked for once the ERP foundation was in place: the look, the
admissions desk, the college data, a host to show it on, and money.

| Feature | Phase | Status | Main files | Tests | Security checks | Manual verification | Known limitation | Last verified |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 13.1 Design system: Liner look and motion | 13 | DONE | `frontend/app/globals.css`, `components/app-shell.tsx`, `components/ui/motion/*` | `motion-primitives` (16 primitives), `dashboard-grid` | Light-only; WCAG AA contrast asserted | All five role dashboards screenshotted desktop and mobile | No dark mode, by request | 2026-09-08 |
| 13.2 Institution branding | 13 | DONE | `apps/branding/`, `frontend/app/admin/branding`, `lib/brand.ts` | `test_branding`, `brand.test` | `platform.configure`; colour validated server-side | Colour picked, applied on next load; logo keeps its own colour | One colour, no logo upload | 2026-09-08 |
| 13.3 Fee quoted at registration, college or employer | 13 | DONE | `apps/students/` (`fee_amount`, `institution`, `institution_kind`, `job_title`, `roll_number`) | `test_student_fees` (17) | Quoting a fee is `student.set_fee_status`, checked in the service | Registered with and without a fee | Superseded as the source of truth by 13.8; kept as "what was quoted" | 2026-09-08 |
| 13.4 Referrals | 13 | DONE | `StudentProfile.referred_by`, `frontend/app/admissions/new` | `test_student_referrals` (12) | Self-referral refused; only an existing student can be credited | Referrer picked in the wizard, shown on the record | No referral reward logic yet — groundwork only | 2026-09-08 |
| 13.5 Register a college student and a working professional differently | 13 | DONE | `components/admissions/student-background-fields.tsx` | `admissions-new` | — | Three-way choice in the wizard; summary on confirm | — | 2026-09-08 |
| 13.6 SITP ACE 2026 workbook import | 13 | DONE | `apps/reporting/sitp.py`, `management/commands/import_sitp_workbooks.py` | `test_sitp_reader`, `test_sitp_import_command` | Goes through the services, so every rule and audit entry applies; invalid rows reported, never dropped | 16 workbooks imported into dev and staging; 616 DSRs approved; report kept under `backups/` | Placeholder trainer and student emails on the `.invalid` domain until real ones are supplied | 2026-09-09 |
| 13.7 Deploy to a host | 13 | DONE | `scripts/deploy.sh`, `docker-compose.staging.yml`, `infra/proxy/Caddyfile` | `make verify ENV=staging` | Host config only in the host's `.env.staging`; refuses a dirty checkout | Running on EC2 (us-east-1) with demo and SITP data | Self-signed certificate on :8443; no domain, no SMTP | 2026-09-09 |
| 13.8 Fee ledger | 13 | DONE | `apps/fees/` (models, services, queries, views), `frontend/components/fees/fee-ledger.tsx`, `components/counsellor/fees-panel.tsx`, `app/my-fees` | `test_fees` (20), `fee-ledger` (8), `fees-panel` (2), `test_data_and_audit_security` (no gateway) | `fee.manage_any` checked in the service; students read only their own; first payment ≥ ₹1,000; no payment above the balance; void needs a reason | Counsellor set fees, discounts, payments, next-due and a void end to end; admin and manager lists show paid/balance/expected date; history timeline read back | Per-enrolment, ad hoc payments, no instalment schedule — by decision (D-011, second amendment) | 2026-09-12 |
| 13.9 Batches on any non-archived course | 13 | DONE | `apps/batches/models.py::Batch.clean`, course pickers | `test_batches_on_draft_courses`, `test_batches_api` | Archived still refused | Batch created on a draft course; publish panel says what publishing now means | — | 2026-09-12 |
| 13.x Manager attention links, approve-only-for-manager, 127.0.0.1 dev origin, worker healthcheck, avatar behind proxy (bug fixes) | 13 | DONE | `apps/reporting/dashboards.py`, `docker-compose.yml`, `frontend/lib/env.ts`, `middleware.ts` | `test_manager_hubs`, `manage-*` | — | Links resolve; healthcheck passes | — | 2026-09-09 |

---


---

## Phase 14 — ERP platform (from 15 September 2026)

The 25-phase programme in `docs/erp/IMPLEMENTATION_PLAN.md`; one row per phase as it lands.

| Feature | Phase | Status | Main files | Tests | Security checks | Manual verification | Known limitation | Last verified |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 14.1 Dynamic authorization: roles as rows, custom roles, the matrix | 14 (ERP P1) | DONE | `apps/authorization/` (models, resolver, sync, services, API), `apps/accounts/roles.py` (`effective_capabilities`), `apps/accounts/models.py` (`custom_role`), migrations `authorization/0001–0002`, `accounts/0006`, `audit/0025`; `frontend/components/roles/*`, `app/admin/roles/*`, custom-role picker on the user page | `test_dynamic_roles` (10), every existing authorization module unchanged (400 in the targeted run; 2737 full suite), `roles-page` (3), `role-builder` (4), `permission-matrix` (1) | Ladder over effective sets; superadmin-only codes refused elsewhere; scope never wider than the kind; refusals are 403 (D-097); version-keyed cache forgotten on write and on commit | Built "Placement coordinator" from Manager without `dsr.view_any`, assigned it, the holder got 403 on `/dsr/` and 200 after widening; disabled role fell back to the kind | Locking and scope enforcement land in Phase 2; `DYNAMIC_ROLES_ENABLED` is the rollback | 2026-09-15 |

| 14.2 Scopes and locking: assigned/own narrowing, permission locks, step-up | 14 (ERP P2) | DONE | `apps/organisation/scoping.py` (`capability=`/`batch_path=`/`own=` on `scope_to_branch`/`scope_to_branch_or_shared`), `apps/authorization/scopes.py` (`effective_scope`, `assigned_batch_ids`), `apps/authorization/models.py` (`ScopeGrant`), `apps/accounts/stepup.py`, `apps/authorization/views.py` (lock/unlock, scope-grant CRUD), migrations `authorization/0003`, `audit/0026`; `frontend/components/roles/step-up-dialog.tsx`, lock controls in `permission-matrix.tsx`, scope-grants card on the user page | `test_scopes_and_locking` (7), full suite unchanged (2748 passed) | Scope narrows only, never widens (checked against the kind's floor); locking is superadmin-only and requires a step-up whose freshness is checked server-side; a scope grant resolves through the caller's own `visible_batches`/`visible_courses`, so a cross-centre id is a 404 | Narrowed a counsellor's `student.view_any` to `assigned`, granted one batch, the holder saw only that batch's roster; locked `admin`'s `audit.view` as superadmin after a step-up, an ordinary admin's attempt to remove it in the Role Builder was refused | `assigned`/`own` are wired for nine capabilities (student, trainer, batch, enrolment, DSR, performance, requirement, session, assignment/project/assessment/exam families); DEPARTMENT scope remains not built | 2026-09-15 |

| 14.3 Policy management: generic policy table, schema registry, resolver, critical-key confirm, Policy Builder screen | 14 (ERP P3) | DONE | `apps/policies/` (models, schemas, resolver, services, serializers, views, urls), `apps/accounts/roles.py` (`POLICY_VIEW`/`POLICY_MANAGE`), `apps/authorization/sync.py` (`policy.manage` lockable), `apps/audit/models.py` (`policy.updated`/`policy.reset`), migrations `policies/0001`, `audit/0027`; frontend `app/admin/policies/page.tsx`, `lib/policies.ts`, `lib/capabilities.ts`/`lib/labels.ts` (`policyView`/`policyManage`, category labels), `components/navigation.ts` (Policies nav entry) | `test_policies` (28), full suite unchanged (2786 passed, 3 skipped — required updating `test_counsellor_rbac.py`'s two generic parametrizations for the new `policy.view` exception, both directions); `tests/unit/policies-page.test.tsx` (10) — full frontend suite 920 passed | Critical keys (`password.*`, `session.max_age_hours`, `deletion.*`) need a fresh step-up and `confirm` naming the key exactly; every value validated against the schema (type, range, choices, the `weights` dict's exact component set); a branch-scoped `policy.view` holder is refused another centre's override (403) and a nonexistent one (404) before any scope check; every write/reset audited with the from/to value; the screen hides Edit/Reset without `policy.manage` and opens the step-up dialog before retrying a refused critical write | Set `password.min_length` to 12 with a step-up and `confirm=min_length`, saw it in force immediately (`policy()` resolver, not just the response) and in `history/`; reset it, the row soft-deleted and the value fell back to 10; a manager's `?branch=<other centre>` was refused, `?branch=<own centre>` fell through to the global row | Verified on staging (list/get/update/history/reset all correct; the critical-key step-up-and-confirm gate refused without step-up, refused with the wrong confirm, and succeeded with both); `authentication`/`password`/`session` now read by Phases 4–5, `risk`/`performance.weights` land at Phases 12–13, `file_upload`/`export`/`communication`/`deletion`/`approval`/`notification` as their features land; the Policy Builder is institution-wide only in this first version, no branch-override picker (D-135) | 2026-09-15 |

| 14.5 MFA: TOTP enrolment, recovery codes, pending-MFA sign-in, a third step-up factor | 14 (ERP P5) | DONE | `apps/accounts/mfa.py` (`MfaDevice`, `RecoveryCode`, `MfaMethod`, TOTP secret encryption via Fernet keyed by `MFA_ENCRYPTION_KEY`, `qr_svg`, `verify_totp_code` with a replay-guarding `last_used_step`, `issue_recovery_codes`/`consume_recovery_code`, `start_enrolment`/`confirm_enrolment`/`disable`/`regenerate_recovery_codes`, `start_pending`/`get_pending_user`/`clear_pending`, `is_required`/`available_methods` reading `policy("authentication", "mfa_required_roles"/"mfa_grace_days")`), `apps/accounts/views.py` (`LoginView` extended for the pending branch, `MfaSendEmailCodeView`, `MfaVerifyView`, `MfaTotpEnrolView`/`ConfirmView`/`DisableView`, `MfaRecoveryRegenerateView`), `apps/accounts/urls.py`, `apps/audit/models.py` (`mfa.enrolled`/`confirmed`/`disabled`/`verified`/`failed`/`recovery_used`/`recovery_regenerated`, `auth.login.pending`), migrations `accounts/0008`, `audit/0029`, `notifications/0004`; `requirements/base.txt` (`pyotp`, `cryptography` promoted from transitive); frontend `components/auth/mfa-verify-form.tsx` (D-137), `components/settings/{mfa-settings-card,recovery-codes-dialog}.tsx`, `lib/mfa.ts`, `app/login/page.tsx`, `app/settings/security/page.tsx` | `tests/test_mfa.py` (12): enrol→confirm→pending-login→verify-by-totp/email/recovery, TOTP replay refused (any step at or before the last accepted one), a used recovery code refused on reuse, a pending session cannot reach an authenticated endpoint, wrong code answers the same generic error for every method, disable and regenerate both require a fresh step-up and genuinely delete rather than soft-delete, `mfa_required_roles=[]` leaves ordinary login untouched; full backend suite unchanged (2834 passed, 3 skipped); `tests/unit/{login-page,mfa-verify-form,mfa-settings-card}.test.tsx` — full frontend suite 959 passed | `OneTimeCode` (Phase 4) reused with `purpose="login"`, no new one-time-code model; the TOTP secret is the one response that ever carries it in the clear (`MfaTotpEnrolView`) and is never logged, audited, or stored unencrypted; recovery codes are SHA-256 hashes, shown once on confirm/regenerate and never retrievable again; disabling MFA requires a fresh step-up and hard-deletes the device and every code — a "restorable" MFA bypass would be a vulnerability, not a convenience; `MFA_ENCRYPTION_KEY` has no default in `hardened.py`/`production.py`, `require_setting` fails closed; a mid-review fix (D- none, a follow-up commit) added `EnforceCSRFMixin` to `MfaSendEmailCodeView`, the one anonymous-POST auth endpoint that had been missing it | On staging: enrolled a TOTP device, confirmed with a code generated in the backend container by `pyotp`, got ten recovery codes back; logged out and back in, got `mfa_required` naming all three methods; a pending session got `403` on `/auth/me/`; a wrong recovery code got the generic `invalid_code`; the real recovery code completed sign-in (`mfa_enabled: true` on `/auth/me/`) and a second attempt to reuse it was refused; disabling without a step-up was `403 step_up_required`, with one it was `204`; a fresh login afterward showed `mfa_enabled: false` again; the audit trail carried only method/purpose/stage, never a code or secret | Policy-driven forced enrolment (`mfa_required_roles`) is wired but no role has been put in the policy yet, so nobody is required into MFA today; TOTP/recovery are not yet offered as extra step-up methods on the existing `StepUpDialog` (D-137, left for a later pass); real SMTP delivery still blocked on the pre-existing "no SMTP credentials" gap | 2026-09-16 |

| 14.6 Session management: `UserSession` inventory, list/revoke/revoke-others, admin revoke, new-device email | 14 (ERP P6) | DONE | `apps/accounts/sessions.py` (`UserSession`, `hash_session_key`, `device_label`/`device_family`, `is_new_device`, `record_login`, `touch_last_seen`, `list_active_sessions`, `revoke_session`, `revoke_other_sessions`, `mark_revoked_by_key`), `apps/accounts/middleware.py` (`TouchSessionActivityMiddleware`), `apps/accounts/views.py` (`LoginView`/`MfaVerifyView` call `sessions.record_login` right after `cycle_key()`, `LogoutView` stamps the row it just ended, new `SessionListView`/`SessionDetailView`/`RevokeOtherSessionsView`), `apps/accounts/user_views.py` (`UserSessionListView`/`UserSessionRevokeView`, `session.view_any`/`session.revoke_any` + fresh step-up), `apps/accounts/services.py` (`revoke_sessions` now also stamps the matching `UserSession` rows), `apps/accounts/roles.py` (`SESSION_VIEW_ANY`/`SESSION_REVOKE_ANY`, admin rung), `apps/authorization/sync.py` (category override to "System", `session.revoke_any` lockable), `apps/notifications/models.py` (`session.new_device`/`session.revoked_by_admin`, both `SECURITY`), `apps/audit/models.py` (`session.revoked`/`session.revoked_by_admin`/`session.new_device_detected`), `config/settings/base.py` (middleware registration), migrations `accounts/0009`, `audit/0030`, `notifications/0005` | `tests/test_sessions.py` (15): a row is written at plain login and at MFA-completed login and not while MFA is pending, `is_current` names the right session across two real logins, revoking the current session is refused (`409 current_session`) and logout/admin-revoke both actually sign the device out, revoke-others leaves exactly the current one, `last_seen_at`'s guard checked directly (untouched inside 5 minutes, touched once stale), new-device detection fires once per genuinely new browser-family/IP-network pair and not on a repeat, an administrator needs both the capability and a fresh step-up and cannot reach another centre's user (404, using a branch-bound custom role holding `session.revoke_any` — the system ADMIN role is deliberately unbounded per D-129); full backend suite green (2862 passed, 3 skipped) after updating two pre-existing flat-cost budgets and `test_authorization_matrix.py`'s student-reachable list for the new self-service routes; frontend not yet built | Only the session key's SHA-256 hash is ever stored, never the key itself (rule §13); revoking one session resolves the underlying Django session row by re-hashing every live session's key (no reverse lookup exists by design) and deletes it, so the device is signed out immediately, not just delisted; the per-session endpoints resolve their target through the caller's own scoped queryset before any step-up check, so a cross-centre id 404s regardless of step-up state; new-device and admin-revoke emails bypass notification preferences (security events, not subscriptions) same as the MFA pair; `session.max_age_hours`/`session.idle_minutes` already exist in the policy schema for a later phase to enforce | Ran the full flow against a live local server: two logins from different browser/OS user-agents, `is_current` correct from each side, self-revoke of the other device actually signed it out, revoke-others left one, an admin without a fresh step-up got `403 step_up_required` and with one got `204`, the target's `/auth/me/` then failed, and the notification/audit rows (`session.new_device_detected` ×3 for three distinct family+network pairs, `session.revoked_by_admin` with the target's id) were exactly as expected — no code path fabricated | Frontend shipped: a sessions card on `/settings/security` (per-device list, this-device badge, revoke, sign-out-elsewhere) and on the admin user detail page (gated on `session.view_any`/`session.revoke_any`, step-up before revoke). On staging: two logins with distinct user-agents produced "Chrome on macOS" and "Firefox on Windows", `revoke-others` from a third session signed out both others (`{"detail": "Signed out of 2 other session(s)."}`), the revoked session's `/auth/me/` then failed, and the audit trail carried three `session.new_device_detected` rows (device label + IP only) and one `session.revoked` row (count only) — no session key or secret anywhere. A real bug was caught by the verify loop before this landed: a stray local `.env` in the worktree was silently overriding deployed/test settings via `os.environ.setdefault()`; fixed by checking the runtime-supplied `DJANGO_ENV` before any `.env` auto-load runs. `session.max_age_hours`/`idle_minutes` remain readable policy keys with no enforcement yet | 2026-09-16 |
| 14.7 Soft delete extended to Announcement; purge now requires a fresh step-up; `AttendanceCorrection` history table + endpoint | 7, 22 | DONE | `apps/announcements/models.py` (`Announcement` converted to `SoftDeleteBaseModel`, `soft_delete_managers`; kept distinct from the pre-existing `archive`/`AnnouncementStatus.ARCHIVED` lifecycle transition, which is editorial, not a removal), `apps/announcements/services.py` (`delete_announcement`), `apps/announcements/{serializers,views,urls}.py` (`AnnouncementDeleteSerializer`, `DeleteAnnouncementView`, `POST /announcements/{id}/delete/`), `apps/common/recovery.py` (`_scoped_by_announcement_branch` added to `BRANCH_PATHS`, reusing `announcements.access._one_centres_notices`; `PurgeRecordView.post` now calls `apps.accounts.stepup.is_fresh`/raises `StepUpRequired` before anything else, same shape as `LockPermissionView`/`MfaTotpDisableView`), `apps/attendance/models.py` (`AttendanceCorrection` — plain `BaseModel`, append-only, never soft-deletable), `apps/attendance/services.py` (`mark_attendance` and `correct_record` both write a history row alongside the existing single-prior-value fields, which stay as-is), `apps/attendance/{serializers,views,urls}.py` (`AttendanceCorrectionSerializer` — `corrected_by_name`, nullable — `AttendanceHistoryView`, `GET /attendance/{record_id}/history/`, gated by combining the two existing view rules rather than inventing one: the owning student/trainer/staff `EnrollmentAttendanceView` already uses, or `SessionAttendanceListView`'s staff-view rule — `ATTENDANCE_VIEW_ANY` or `can_take_attendance`), migrations `announcements/0004`, `attendance/0002` | `tests/test_soft_delete.py`'s generic sweep now covers `Announcement` with no new file needed; new `tests/test_announcements_recovery.py` (delete endpoint removes it from the board and the bin holds it, a blank reason is refused, a manager outside the notice's centre gets a 404 through `manageable_announcements`, delete is distinct from archive, and `_restorable_by` directly proves the new `BRANCH_PATHS` callable refuses a bounded caller outside the centre while an unbounded admin succeeds); `tests/test_attendance.py` extended (re-marking and `correct_record` each write a history row with the right from/to/reason/actor, the endpoint returns newest-first, a trainer who takes the class and the owning student can both see it, another student and another centre's bounded trainer get 403/404); `tests/test_recovery.py`'s purge tests updated to step up first, plus a new one proving an un-stepped-up superadmin gets `403 step_up_required`; full backend suite green (2873 passed, 3 skipped); `makemigrations --check --dry-run` clean; `ruff`/`bandit`/`check --deploy` clean | Purge's step-up check runs before the record lookup, so a superadmin without a fresh step-up gets the same refusal whether or not the id exists — no existence oracle through the new gate; the announcement branch rule mirrors `manageable_announcements`' *write* scoping (no institution-wide carve-out), not the wider read scoping, since restoring is a write; `AttendanceCorrection` is intentionally not soft-deletable — a history log with a delete button is not a history log | Not yet run against a live server in this pass (backend-only implementer) | A `components/academics/attendance-history.tsx` panel and `frontend/lib/academics.ts::getAttendanceHistory` landed alongside this backend work (wired into `/my-attendance`'s corrected rows); the recovery bin's purge-button step-up wiring (`components/recovery/purge-control.tsx`) does retry the same way the permission matrix's lock action does, confirmed by its own test and by hand on staging: created and deleted a test announcement, the bin listed it under `announcements.announcement`, restored it; purge without a fresh step-up answered `403 step_up_required`, with one it answered `204`; `GET /attendance/<id>/history/` returned `200 []` for an uncorrected record. No new route shipped (both surfaces are existing pages), so the full six-role crawl was skipped per the leaner process — a single-role check of `/my-attendance` and `/admin/recovery` came back clean | 2026-09-16 |
| 14.8 Dynamic forms: `FormDefinition`/`FormVersion`/`FormField`/`FormResponse`, 17-type validator, publish/unpublish with immutability, Form Builder UI | 8 | DONE | `apps/forms/` (new app: `models.py`, `validation.py` — the one place all 17 field types are checked, `services.py` — `create_definition`, `create_draft_version`, `set_fields`, `publish_version`, `unpublish_version`, `validate_response`, `submit_response`, `views.py`, `urls.py`, mounted at `/api/v1/forms/`), `apps/forms/migrations/0001_initial.py`, `0002_seed_catalog.py` (data migration, seeds all activity forms plus `student-custom`/`registration-extra` from `FORM_CATALOG.md`, each published v1), `apps/accounts/roles.py` (`FORM_VIEW`/`FORM_MANAGE`), `apps/authorization/sync.py` (`form.manage` lockable), `apps/audit/models.py` (five `form.*` audit actions), `config/api_urls.py`/`config/settings/base.py` wiring; frontend `lib/forms.ts`, `types/api.ts`, `components/forms/{field-renderer,field-editor,form-detail}.tsx`, `app/admin/forms/{page,[slug]/page}.tsx`, nav entry gated on `form.manage` | `backend/tests/test_forms.py` (36: one pass/fail pair per field type, immutability — editing a published version's fields is refused 409, versioning — schema-hash publish/unchanged-refusal/clone-from-published, relation-field scoping against the caller's own `visible_*`, capability gating, seed data); frontend `tests/unit/{forms-list-page,form-field-editor,field-renderer,form-detail-publish}.test.tsx`; full backend suite green (2920 passed, 3 skipped); frontend 1005 passed | Adversarial review caught a real check-then-act race in `publish_version` (two concurrent publishes of different draft versions of the same definition could both pass the "what's currently published" check before either wrote, leaving two versions simultaneously published) — fixed with `select_for_update()` across the definition's own version rows before any read, same pattern as Phase 4's OTP race; `ruff`/`bandit`/`check --deploy` clean; relation fields resolve only through the caller's `visible_*` queryset, never an unscoped lookup | Verified on staging: logged in as superadmin, `GET /api/v1/forms/` returned the seeded catalog (15 activity forms + `student-custom` + `registration-extra`, all published v1), `GET /api/v1/forms/student-custom/` showed the correct empty published version, `POST /api/v1/forms/code-review/preview/` with an out-of-range decimal and a missing required field returned both field errors at once (`score`, `comments`) without storing anything | File/image validation resolves against an injectable seam (`UPLOAD_RESOLVER`) rather than a concrete upload model, since no generic queryable upload registry exists yet in this codebase — a later phase wiring real uploads to forms needs to set this resolver; relation fields only resolve `student`/`trainer`/`batch` (the validator's specified set), so a seeded field referencing `Module`/`Lesson`/`StudentProject` correctly fails validation with "Unsupported relation model" rather than silently accepting, until those apps grow a `visible_*` helper | `/admin/forms` nav entry (added after Policies) confirmed live in a full crawl — 0 problems for superadmin (41 routes) — and directly verified via a page-text dump showing "Forms" in the Institution nav group | 2026-09-16 |
| 14.9 Activity engine: `ActivityType`/`Activity`/`ActivityHistory`, twelve-status lifecycle, complete/review reusing Phase 8's validator, seeded 18-type catalog, calendar source, overdue/reminder beat tasks | 9 | DONE | `apps/work/` (new app: `models.py`, `transitions.py` — the explicit edge table with per-transition actor/precondition checks, `access.py` — `visible_activities`, `can_assign`/`can_complete`/`can_review`, a capability-free `_trainer_teaches_batch` per-record path, `services.py` — `create_activity` (client_key idempotency via an advisory lock, same pattern as `apps/accounts/otp.py`), `transition_activity`, `complete_activity` (reuses Phase 8's `apps.forms.services.validate_response`/`submit_response`, derives score via the new `FormVersion.performance_field()`), `review_activity`, `delete_activity`, `mark_overdue_and_missed`/`send_activity_reminders`, `signals.py` (one `activity_changed` signal — Phases 13/14 attach receivers later, no direct dependency), `views.py`, `urls.py`, `tasks.py`, mounted at `/api/v1/activities/` and `/api/v1/activity-types/`), `apps/work/migrations/0001_initial.py`, `0002_seed_catalog.py` (18 types from `ACTIVITY_CATALOG.md`, idempotent, resolves `form` by slug against `apps.forms`), `apps/accounts/roles.py` (7 `activity.*`/`activity_type.manage` capabilities), `apps/dashboards/calendar.py` (`EventKind.ACTIVITY_DUE` + a scoped source), `config/settings/base.py` (`work.mark_overdue`/`work.reminders` beat schedule), `apps/forms/models.py` (`FormVersion.performance_field()`), `apps/audit/models.py`/`apps/notifications/models.py` (new action/kind members); frontend `lib/work.ts`, `app/admin/activity-types/page.tsx`, `app/activities/page.tsx`, `components/work/activity-drawer.tsx` (history, the pinned form via Phase 8's `field-renderer`, a transition action bar, a Complete form, a self-review-hiding Review panel), `components/require-auth.tsx` (new `roles` prop — an escape hatch for a role whose reach the backend resolves per-record rather than by capability) | `backend/tests/{test_work_engine,test_work_transitions,test_work_visibility}.py` (73: type-allowlist enforcement, a genuine concurrent client_key race, every transition-table edge's actor/precondition, capability+scope gating across all/branch/assigned tiers, student-visibility of activities and form fields); frontend `tests/unit/{activities-page,activity-drawer,activity-types-page}.test.tsx` (13, including one added during my own follow-up fix); full backend suite green (3022 passed, 3 skipped); frontend 1023 passed | Adversarial review caught two real authorization **blockers** before this landed: (1) `PATCH .../assigned_to` checked only "may this caller reassign" and skipped the `allowed_assignee_roles`/scope check `create_activity` enforces on the same field — an assign-holder could reassign to literally any active user, including a student, who would then satisfy `visible_activities()`/`can_complete()` and complete staff-only work; fixed by routing both paths through one `validate_assignee`. (2) `StudentActivityListView`'s staff branch dropped the activity engine's own branch/assigned scoping entirely once the caller could see the student profile at all — fixed by intersecting with `access.visible_activities(user)`, mirroring `apps.dsr.views.BatchDSRListView`'s established pattern. Also fixed two majors: history notes/`changes` and the derived `score`/`max_score`/`result`/`review_note` now redact for a student caller unless the underlying form field is itself `visible_to_student` (previously unfiltered, unlike `form`/`form_values`); and a minor: the generic `/transition/` endpoint no longer accepts `UNDER_REVIEW`'s edges, so approval attribution can only come from `POST .../review/`. `ruff`/`bandit`/migration-check/`check --deploy` clean throughout | Verified on staging as manager: created a `code-review` activity for a real demo student (form pinned with 6 fields, `available_transitions` correctly `["cancelled","planned"]` for a fresh `draft`); reproduced finding (1)'s fix live — `PATCH assigned_to` to the student's own user id was refused `400 "This role may not be assigned this activity type."`; confirmed a scoped trainer correctly gets `403 outside_authority` on a student outside their reach before any activity-level scoping even runs; transitioned to `cancelled` and confirmed the history/audit row recorded actor+note correctly, then left it cancelled (no stray open test data) | A related gap surfaced independently, not by the workflow's review: the backend resolves a trainer's own reach per-record (`_trainer_teaches_batch`, capability-free, to avoid breaking an existing role-hierarchy invariant that trainer holds only `BASE_CAPABILITIES`), but the frontend's nav entry, page guard, and the drawer's Complete/Assign actions all gated on capabilities a trainer never holds — trainers, the primary assignee for most seeded types, would have seen no way into the feature at all. Fixed as a small follow-up (not a new workflow): `RequireAuth` gained an optional `roles` escape hatch, the nav entry and the `/activities` page now also allow `role: "trainer"`, and the drawer's `canComplete`/`canAssign` also pass for the activity's own assignee/creator, mirroring `access.can_complete`/`can_assign` exactly; the drawer's own stale test asserting a trainer should be denied was corrected, and a positive trainer-allowed test was added. `automation_run` and `parent` fields exist on the model but nothing populates them yet (Phase 14); `risk_effect`/`next_action` are stored but not dispatched (Phases 13/14 read them later) | 2026-09-16 |
| 14.10 Activity timeline: `work/timeline.py` composed read model over 8 sources, cursor pagination, `GET /students/{id}/timeline/`, a reusable Timeline component | 10 | DONE | `apps/work/timeline.py` (new — `TimelineEntry` dataclass, `TIMELINE_SOURCES` registry following `apps/dashboards/calendar.py`'s exact multi-source pattern; sources: enrolment+transfer (`Enrollment.status`/`transferred_to`, not a separate model), attendance day, DSR, assessment result, assignment submission, project state, activity (also covers the `feedback`/`review` `ActivityType.category` values — not separate sources), certificate; `decode_cursor`/`encode_cursor` — opaque `(occurred_at, id)` cursor, `timeline_for(user, student, kinds, since, until, cursor, page_size)` merges/sorts/truncates), `apps/work/timeline_views.py` (new — `GET /students/{id}/timeline/`, reuses the same "can this caller see this student" gate `StudentActivityListView` (Phase 9) already uses); frontend `lib/timeline.ts`, `components/students/timeline.tsx` (kind-filter chips, day-grouped list, load-more via `next_cursor`), `app/students/[id]/timeline/page.tsx` (new route — not yet linked from Student 360, Phase 11 embeds it), `lib/labels.ts` (kind→icon/label map with a generic fallback for an unrecognized kind, so Phase 19's future communication-delivery source needs no frontend change) | `backend/tests/test_timeline.py` (9: flat cross-source ordering, cursor pagination resumes correctly across a same-second tie via the `id` half of the cursor, the query-budget assertion — now seeded with a real row in **every** source after review fixed a test that originally only exercised one, a scoped caller's per-source visibility still applies after the top-level student-visibility check passes, `kinds` filtering skips querying excluded sources, a student's own timeline drops `student_visible=False` activity entries entirely); frontend `tests/unit/timeline.test.tsx`; full backend suite green (3031 passed, 3 skipped); frontend 1029 passed | Adversarial review scrutinized this phase harder than a typical read-only composition, given Phase 9's two authorization blockers in a sibling app — the per-source scoping came back clean this time (every source correctly calls its own domain's `visible_*` helper), but found two real **majors**: (1) `decode_cursor` accepted a naive (no UTC offset) ISO timestamp, which later crashed with an uncaught `TypeError` comparing it against timezone-aware entries — a hand-crafted cursor 500'd instead of degrading gracefully; fixed by rejecting a naive timestamp inside the same validation block that already catches a malformed cursor, so it returns `400 "This cursor is not valid."` like any other bad cursor. (2) the query-budget test only seeded one of eight sources, so a missing `select_related` in any of the other seven would never have been caught; fixed by seeding a real row in every source before asserting the query count. `ruff`/`bandit`/migration-check (none expected — no new models)/`check --deploy` clean throughout | Verified on staging as manager: `GET /students/{id}/timeline/` returned the real `activity_cancelled` entry from Phase 9's own smoke-test activity, correctly attributed to its actor; reproduced the exact naive-datetime cursor from the review finding (`base64("2026-01-01T00:00:00\|activity:5")`) live and confirmed it now answers `400 {"cursor": ["This cursor is not valid."]}`, not a 500; loaded the new `/students/{id}/timeline/page.tsx` route as manager and confirmed it renders the student header, all seven kind-filter chips, and the same timeline entry with no console/API errors beyond the normal pre-login 401/403 probes | The new timeline route isn't linked from any nav or student page yet — Phase 11 (Student 360) is what embeds `components/students/timeline.tsx`, so reaching it today requires typing the URL directly; no "communication delivery" source exists yet since the `communication` app is Phase 19's, documented in `timeline.py`'s module docstring as an additive follow-up (append one source function to `TIMELINE_SOURCES`, no other change needed) | 2026-09-16 |
| 14.11 Student 360 (`GET /students/{id}/360/`, one composed call with scope-aware caching), global search across 10 domains, command palette, saved filters, unsaved-changes hook | 11 | DONE | `apps/students/student_360.py` (new — `viewer_scope_key` for the 1-minute per-scope-tier cache, `build()` composing profile/enrollment/batch/trainer/counsellor/progress/attendance/counts/fee_status/`recent_activities` (reuses `apps.work.access.visible_activities`, Phase 9's fix) plus inert `performance`/`risk`/`next_actions` placeholders for Phases 12/13/14), `apps/search/` (new app — `services.py`'s 10 sources — students/trainers/users/batches/courses/activities/assessments/assignments/projects/dsrs — each through that domain's own `visible_*`/capability gate; `views.py`'s `GlobalSearchView`, `SEARCH_GLOBAL` in `BASE_CAPABILITIES`), `apps/reporting/models.py` (`SavedFilter`, migration `0003`), `apps/reporting/views.py` (`SavedFilterListCreateView`/`SavedFilterDetailView`, strictly `request.user`-scoped, a guessed id 404s), `apps/common/throttling.py` (`SearchThrottle`), `apps/audit/models.py` (`search.performed`, counts-only context); frontend `app/students/[id]/page.tsx` (tabbed 360 page — Overview/Activities/Timeline, extends rather than duplicates: reuses `StudentPerformance` with a new `variant="embedded"` prop, embeds Phase 10's `components/students/timeline.tsx`, a new `student-activities-tab.tsx`; header's score/risk show "Not yet computed"/"No risk signals" until Phases 12/13 land), `components/command-palette.tsx` (built on the existing `Dialog`, no new `cmdk` dependency; `mod+k`, keyboard-only, local nav items client-side + `/search/` debounced past 2 chars), redirect wiring (`app/manage/students/[enrollmentId]/page.tsx` → `/students/{id}?tab=enrollment`; `app/admissions/[studentId]/page.tsx` keeps its own fee-ledger view, adds a "View 360 profile" link instead per D-128's carve-out), `hooks/use-unsaved-changes.ts` (wired into `/activities`'s new "Save this filter" popover), `lib/{student-360,search,saved-filters}.ts` | `backend/tests/test_student_360.py`, `test_search.py` (per-domain scoping proven directly for students/batches/users/activities, code-reviewed correct for the remaining six), saved-filters tests (per-user isolation, a guessed id 404s); frontend `tests/unit/{student-360-page,command-palette,use-unsaved-changes}.test.tsx`; full backend suite green (3076 passed, 3 skipped); frontend 1051 passed | This phase's workflow was interrupted by a session restart mid-run and resumed via `Workflow({scriptPath, resumeFromRunId})` — the resumed run correctly replayed the frontend implementer's already-completed, already-verified work from cache and only re-ran the backend implementer and the Verify/Review/Finalize stages live, exactly the recovery pattern this programme has used for transient failures since Phase 5. Adversarial review (held to Phase-9 scrutiny given global search's cross-domain disclosure risk) found no blockers or majors this time — only two minors and a nit, all confirmed by my own direct reading of the code rather than taken on trust: (1) the cache key's documented residual imprecision (a custom role narrowing a *different* capability tighter than `student.view_any` could share a cache entry within its own tier for up to 60s) never crosses a real authority boundary (all/branch/assigned/own), so accepted as-is per the code's own reasoning; (2) `test_search.py` proves per-domain scoping directly for 4 of 10 sources, code-reviewed correct for the rest (I independently re-read all 10 source functions myself and confirmed each calls its own `visible_*`); (3) the 360 page's client-side gate is stricter than the API (a teaching trainer or the student themself can call the endpoint but the page's `RequireAuth` doesn't let them in) — fail-closed, matching the pre-existing timeline page's same restriction, not a Phase 11 regression. `ruff`/`bandit`/migration-check/`check --deploy` clean throughout | Verified on staging: searched "kabir" as `trainer1` (scoped to batches they don't teach) and got zero hits across all ten source groups; the same search as `manager` (broader scope) found the real student both in the `students` and `users` groups; `GET /students/{id}/360/` returned real profile/enrollment/attendance/fee data plus correctly inert `performance: {components: [], overall_score: null}`, `risk: {level: "none", triggered: []}`, `next_actions: []`, and `recent_activities` correctly surfacing the same Phase 9 smoke-test activity as before; created, listed, and deleted a real saved filter end to end (`204` on delete) | The 360 page's `recent_activities` links (`/activities?id={pk}`) don't yet deep-link into the Activities page's drawer, since that page doesn't read an `id` query param — an inert link, not a broken one, left for Phase 9's own page to pick up later if wanted; an existing pre-ERP `apps.performance` app already computes a score by a different method — Phase 12 needs to either replace or reconcile with it rather than leaving two competing numbers | 2026-09-16 |
| 14.12 Weighted performance components with provenance (ADR-10): `components[]`/`overall_score`, a new `activity` component from Phase 9 data, `PerformanceReview` extension, wired into Student 360 | 12 | DONE | `apps/performance/engine.py` (`student_performance`/`student_performance_bulk` now return `components: [{key, label, weight, value, contribution, sources}]` alongside the existing per-domain breakdowns; `_weighted_overall()` computes the weighted mean of components with data, with `contribution` values that sum to `overall_score`; a new `_activity_component()` — the weighted mean of COMPLETED/APPROVED activities' normalised (`score/max_score×100`) scores, weighted by each `ActivityType.performance_weight`, `sources` listing `{type, score, date, trainer}` per contributing activity), `apps/policies/schemas.py` (`PERFORMANCE_COMPONENTS` gains `"activity"`), `apps/work/models.py` (`ActivityType.performance_weight` gained a DB `CheckConstraint` **and** serializer `min_value=0` — non-negative, matching the top-level policy's own bound), `apps/policies/resolver.py` (`_reconcile()` — any persisted "weights"-typed policy value now backfills missing keys from the schema's current default at read time, so a `performance.weights` row saved before this phase's `"activity"` key existed doesn't silently pin it to permanent weight-0), `apps/performance/models.py` (`PerformanceReview` gains `review_type`, `score`, `recommendations`, `next_review_at`, `status`, plus a `weaknesses` property that is a true passthrough of the existing `concerns` column, not a second field), migration `performance/0002`, `apps/students/student_360.py` (the `performance` placeholder is now real data from `student_performance` for the student's active enrollment, still the inert placeholder shape when there is none); frontend `app/students/[id]/page.tsx`'s "why" popover now renders `sources` for the activity component and shows `weight` as a plain relative number instead of a misleading `formatPercent` | `backend/tests/test_performance_engine.py` (the regression test recomputes the plain mean independently from three real, non-trivial component values — attendance, assessment, progress, no activity data — and asserts `overall_score` matches exactly, proving the equal-weights-reproduces-today contract is real rather than vacuous; the activity component's weighted-by-type-weight math, a `performance_weight=0` type contributing exactly nothing, COMPLETED/APPROVED-only filtering, `contribution` values summing to `overall_score`), `test_reviews.py` extension (`weaknesses`/`concerns` alias round-trips both directions); full backend suite green (3100 passed, 3 skipped); frontend 1052 passed | Adversarial review, told to hunt for silently-wrong numbers rather than authorization bugs, found one real **major** and caught it before it shipped: `ActivityType.performance_weight` had no non-negative validation anywhere (model, both write serializers), so an admin setting a negative weight could sign-flip or corrupt the activity/overall score with no error — inconsistent with the top-level `performance.weights` policy, which was already bounded; fixed with both a DB constraint and serializer validation. Also fixed two minors: the stale-policy-row gap described above (a real forward-compatibility risk, not hypothetical — any pre-Phase-12 `performance.weights` row would have hit it the moment this phase shipped), and the misleading `weight×100%` popover label, which was inert scaffolding until this phase made it user-visible with real, non-uniform weights for the first time. A pre-existing, unrelated one-line typo (`frontend/app/page.tsx`'s homepage heading corrupted to "Grra LMS") was caught twice — once by Verify (flagged, correctly left untouched as out of scope) and again by Finalize, which reverted it in the working tree before committing rather than let it slip into the phase's commit. `ruff`/`bandit`/migration-check/`check --deploy` clean throughout | Independently re-ran the full backend suite (3100 passed) and read the actual regression test, the weight-validation fix (DB constraint + serializer), and the resolver's `_reconcile()` fix myself rather than trusting the reports. Verified on staging: `performance.weights` policy currently reads as its own default with the `activity` key already present (`is_default: true`, no stale row exists yet, so the reconciliation fix has nothing to reconcile today but is proven correct by direct code reading and its own test); `GET /students/{id}/360/` for two different demo students both returned the correctly-inert placeholder (`components: [], overall_score: null`) — traced this to `enrollment: null` on both, i.e. these demo accounts have no currently-active enrollment on staging, not a Phase 12 regression (the "nothing to measure" rule behaves identically whether the underlying cause is no data or no enrollment) | No demo student on staging currently has an active enrollment with real attendance/assessment/activity data to show the *non-empty* path live end-to-end; the regression test and unit tests are the proof for that path until a staging fixture exists with one. `apps.performance`'s pre-ERP score-computation approach is exactly what this phase extended (not a second, competing engine) — resolved, not left open | 2026-09-16 |
| 14.13 Risk engine: `RiskState` stored verdicts (ADR-11), three new rules (activity/project/placement), debounced recompute, `RISK_CHANGED` event, `/risk/summary/`, wired into Student 360 | 13 | DONE | `apps/performance/risk.py` (three new rules — `_activity_risk` (overdue count OR last completed/approved score below threshold, worse-of-two severity), `_project_risk` (overdue `StudentProject` count), `_placement_risk` (placement-category activity average) — each through the existing `RiskOutcome`/`_severity_for_deficit`/`_severity_for_excess` machinery, never a hand-rolled comparison; new `level_for()` deriving none/warning/critical as the worst severity among triggered rules, folded into `evaluate()`'s existing return shape), `apps/performance/models.py` (`RiskState` — one live verdict per enrolment, `previous_level`/`previous_triggered` as the built-in history ADR-11 asks for), migration `performance/0003`, `apps/performance/services.py` (`recompute_risk` — `@transaction.atomic` + `select_for_update`, moves current into previous before overwriting, fires a new `RISK_CHANGED` signal (`apps/performance/signals.py`, mirrors Phase 9's `activity_changed` shape for Phase 14) and a notification only when level/triggered actually changed; `risk_summary` — three fixed queries scoped by the existing `access.visible_enrollments_for_performance`, never a risk-only parallel scoping rule), `apps/performance/tasks.py` (`schedule_recompute`/`recompute_risk` — `cache.add` as a genuine atomic add-if-absent debounce lock, 5-second coalescing window, cleared in the task's own `finally`, a 120s TTL as a worker-outage safety net only), `apps/performance/receivers.py` (a receiver on Phase 9's `activity_changed` signal — no edit to `apps.work` needed), wiring into `apps/attendance/assessments/assignments` services' write-paths, `apps/policies/schemas.py` (four new `risk_*` threshold keys under the existing `"risk"` category), `apps/students/student_360.py`'s `risk` placeholder is now `services.risk_state_for` — computed synchronously on a first-ever read so a brand-new enrolment never waits on the async task; frontend: a Risk tab on the Student 360 page rendering the full triggered-rule breakdown with severity badges, reusing Phase 11's risk badge/popover structure | `backend/tests/test_risk_change_detection.py` (a same-verdict recompute does NOT fire `RISK_CHANGED`/a notification; a genuine level change moves current into previous and fires both; the debounce's `schedule_recompute` return value directly proves N rapid triggers coalesce to one scheduled task), risk-rule tests (triggered/not-triggered pairs, severity thresholds, `level_for`'s derivation), a flat-cost test for `/risk/summary/`, `test_branch_scoping_api.py`'s manager-dashboard query ceiling raised 35→45 with a documented reason (four more fixed, per-request-memoised policy-key reads, not a per-row cost); full backend suite green (3144 passed, 3 skipped); frontend 1055 passed | **Recovery note:** the launching workflow's backend implementer stalled repeatedly across 6 retry attempts and the workflow ultimately crashed (`TypeError: null is not an object`) before its own Verify/Review/Finalize stages completed — traced to a local Docker/Postgres outage during a session gap (the `grras-lms-db-1` container had stopped and Docker itself wasn't running), not a code problem. The implementer's on-disk work was already complete and syntactically sound; restarted Docker, ran the full 10-command verify checklist myself (all clean), and — since the automated Review stage never ran at all — did a full manual adversarial review in its place before committing: read and confirmed correct the debounce's atomicity (`cache.add`, not check-then-act), `RISK_CHANGED`'s change-gating (`if changed:`, comparing against the true previous verdict), `level_for`'s max-severity derivation, all three new rules' zero/None-data handling (an overdue count of exactly 0 is a real "no risk" answer; a `None` last-score/average is "nothing to measure yet", never misread as a trigger), and `/risk/summary/`'s scoping. No blockers found — this implementer's work held up to the same scrutiny an automated review would have applied | Verified on staging: `GET /api/v1/risk/summary/` as manager returned real scoped counts (`{"none": 1057, "warning": 0, "critical": 0}`, empty `top`, consistent with no demo enrolment currently triggering anything); `GET /students/{id}/360/`'s `risk` field now returns a real computed verdict (`{"level": "none", "triggered": []}`) rather than Phase 11's static placeholder; loaded the Risk tab (`?tab=risk`) on a real student page and confirmed it renders "Current risk level" / "No risk signals" cleanly with no console/API errors beyond the normal pre-login probes | The Risk tab's empty-state sentence names only the four legacy rules (attendance, assessment average, missed assignments, progress) rather than all seven — a cosmetic copy gap, not a functional one, since `triggered: []` is correctly empty either way; worth a one-line fix later. Same staging-data limitation as Phase 12: no demo enrolment has real attendance/assessment/activity data to exercise a genuinely triggered warning/critical verdict live, though the rule logic itself is proven by unit tests with real fixtures | 2026-09-16 |
| 14.14 Next-action automation (ADR-13): app `automation` — `AutomationRule`/`AutomationRun`, a 7-trigger/8-operator evaluator, 6 actions, idempotency/depth/rate guards, save-time permission checks, 9 seeded rules, an Automation Builder with dry-run | 14 | DONE | `apps/automation/` (new app: `models.py` — `AutomationRule` [soft], `AutomationRun` [plain, append-only, DB `UniqueConstraint(rule, occurrence_key)` as the idempotency guard]; `evaluator.py` — `evaluate_conditions`/`context_for`, a missing path is always `false`, never an error; `actions.py` — `create_activity` (routes through `apps.work.services.create_activity`, inheriting its scope/role validation, never a direct model write), `send_notification` (safe `{{path}}` substitution into `apps.notifications.services.notify`, never `eval`/format-string), `send_email`/`send_whatsapp` (always `skipped`, documented reason: no `Delivery`/template system until Phase 19 — deliberately not faked), `create_review` (draft `PerformanceReview`), `flag_risk` (manual `RiskState` override); `services.py` — `dispatch()` (rate guard + idempotent run creation inside one `transaction.atomic()` block with `select_for_update()` on the rule row, so a rate-guard failure rolls back the whole attempt rather than stranding a claimed occurrence; depth guard at `MAX_DEPTH=3`; each action's own failure caught per-action, never propagated), `create_rule`/`update_rule`/`activate_rule` (save-time and activation-time `_require_author_permissions` — a rule cannot be saved or activated with an action needing a capability its actor doesn't hold), `sync_rule_authors` (pauses a rule whose author has since lost a required permission; explicitly skips unauthored — seeded — rows); `receivers.py` on Phase 9's `activity_changed` and Phase 13's `RISK_CHANGED` signals; wiring into `apps.work.tasks` (overdue triggers) and `apps.assessments.services.record_result` (`ASSESSMENT_FAILED`); migrations `0001` (models) + `0002` (the 9 seeded rules from `AUTOMATION_CATALOG.md`, via a frozen migration-local shape validator — never importing live `apps.automation.services`/`.actions` into a migration); `apps.accounts.roles` (`AUTOMATION_MANAGE` — already existed from the catalog, `COMMUNICATION_SEND` — new); `apps.work.models.Activity.automation_run` (additive FK, deliberately absent since Phase 9 until `AutomationRun` existed); `apps.performance.models.RiskState` gains `manual_override`/`manual_override_level`/`manual_override_reason`/`manual_override_expires_at`/`manual_override_set_by` plus a `manual_override_active` property (derives "still in force" from the expiry, not a stored flag alone); frontend: `app/admin/automation/{page,[id]/page}.tsx`, `components/automation/{rule-builder,condition-editor,action-editor}.tsx`, dry-run + an explicit activate confirmation naming the matched-event count, nav entry under Configuration | `backend/tests/test_automation.py` (one test per trigger/action, a genuine concurrent-race idempotency test, the depth guard actually bounding a two-rule cycle — not an ad-hoc cycle detector, the save-time permission check, `TestListCreateViewOverHTTP` — added after a staging bug, see Known limitations); full backend suite green (3179 passed, 3 skipped); frontend 117 files / 1069 tests | Adversarial review, held to Phase-9-level scrutiny given real record creation/assignment on behalf of "the system", found **3 majors + 2 minors + 1 nit** — the richest single review pass this programme has run, and every finding was real: (1) `flag_risk` was gated by the read-only `PERFORMANCE_VIEW_ANY` instead of a write capability — fixed to `REVIEW_MANAGE_ANY`; (2) the manual-override system was non-functional across three interacting sub-bugs at once — `recompute_risk` unconditionally overwrote an active override, the override fields were never cleared once superseded, and `student_360._risk_for` computed risk live (bypassing the persisted override entirely) while `/risk/summary/` read the persisted row — so a manual flag was invisible on the one screen a person actually checks; fixed as one coherent change: an active override is left standing (only `numbers` refresh) until it expires, at which point the next recompute clears it, and both read paths now agree on which value is authoritative; (3) `dispatch()`'s rate-guard section had no error handling, so a policy-lookup or DB error would propagate uncaught, permanently and silently losing that occurrence once the idempotency guard's unique key was already claimed — fixed by moving the rate check, the run-row creation, and a `select_for_update()` on the rule row into one `transaction.atomic()` block, so any failure rolls the whole attempt back cleanly, and (4) the same block closes the rate guard's own check-then-act race the DB-constraint-backed idempotency guard right beside it didn't share; (5) **a real privilege-escalation path**: `activate_rule()` re-checked the rule's *original author's* permissions rather than the activator's own, so anyone holding only `automation.manage` could activate someone else's draft and have it start executing `communication.send`/`review.manage_any`-gated actions without holding those capabilities themselves — fixed to check the activator; (6) the seed migration bypassed all permission validation via a raw `.create()` with `created_by=None` — exactly the "seeded some other way" bypass path the review brief named by name — resolved with a frozen, migration-local re-implementation of the shape validator (a migration must never import live service code) plus a documented, deliberate exemption from author-permission checking for unauthored system rows (there is no author to check); (7) `is_system=False` on all 9 seeded rows, initially flagged as contradicting the field's docstring — on inspection this is exactly what `AUTOMATION_CATALOG.md` itself specifies ("`is_system=False`, editable... none is locked"), so the docstring was corrected to match the catalog rather than the seed data changed. `ruff`/`bandit`/migration-check/`check --deploy` clean throughout | Independently re-ran the full backend suite (3179 passed) and read the actual code for all seven findings myself given the severity, confirming each genuinely fixed rather than trusting the report — including tracing `student_360._risk_for`'s corrected level-override logic and `dispatch()`'s new atomic-block structure line by line. **Found and fixed one more bug myself, after Finalize, that nothing in the automated pipeline caught**: `GET /api/v1/automation-rules/` 500'd on first real staging request with `AssertionError: 'AutomationRuleListCreateView' should either include a serializer_class...` — the view's `@extend_schema(responses=...)` satisfied drf-spectacular's schema-generation check (so `check --deploy` stayed clean) but DRF's actual `ListAPIView.get()` still calls `get_serializer_class()` at runtime regardless; no test in the phase's own suite ever called `GET` through the view layer (every test called `services.*` directly), so 3179 passing tests and a clean deploy check still shipped a 500. Fixed with the missing `serializer_class`, a new `client.get()`-shaped regression test, and a permanent note in `docs/erp/AGENT_PLAYBOOK.md` (`fix(erp-p14)` follow-up commit). After the fix, verified live: all 9 seeded rules listed as `active` exactly matching the catalog by name/trigger, the dry-run endpoint returns `200 []` cleanly | The full `ACTIVITY_COMPLETED → create_activity` pipeline is proven by unit tests with real fixtures but not exercised live end-to-end on staging — no demo batch currently has a trainer assigned, so a mock-interview activity cannot reach an assignable state to complete (same batch/trainer data gap Phase 12/13 already hit); `send_email`/`send_whatsapp` remain permanently `skipped` until Phase 19 builds the `communication` app and its template registry, by design, not as a gap. This phase's own workflow launch failed twice before starting due to a script-authoring mistake (a markdown-style inline backtick inside a backtick-delimited prompt string terminated the template literal early; the fix for that accidentally merged one prompt constant's entire body into another, silently undefining it) — caught by noticing the backend implementer never once logged a "started" event across two full attempts, diagnosed by reading the saved script file directly, and fixed by splitting the merged constants back apart | 2026-09-17 |
| 14.15 DSR integration: "create an activity from this class" (`POST /dsr/{id}/create-activity/`), `dsr.rejected` notification, DSR change history (`GET /dsr/{id}/history/`) | 15 | DONE | `apps/dsr/views.py` (`DSRCreateActivityView` — a convenience wrapper, not a new creation path: resolves the caller-named student's active/completed enrollment on the report's own batch, 400s if not enrolled, then calls `apps.work.services.create_activity` directly, inheriting its allowed-role/scope validation unchanged; `DSRHistoryView` — a queryset over `AuditLog` filtered by `resource_type="dsr"`, gated by the same `_dsr_for` scoping the detail view already uses, per `DATA_MODEL.md`'s "no table, ChangeHistory is a queryset over AuditLog" note), `apps/dsr/services.py` (`review_dsr` calls a new `_notify_rejected` helper — matches the existing `notify()` call shape from `apps.performance.services`/`apps.work.services` — only when the decision is `REJECTED`, including the reviewer's `manager_comments`), `apps/notifications/models.py` (`NotificationKind.DSR_REJECTED`, additive migration); frontend extends the existing `dsr-panel.tsx`/`dsr-review-queue.tsx` (no new page) with a "Create activity" action and a History section, reusing Phase 9's activity-type list and Phase 7's attendance-history UI pattern | Extended `backend/tests/test_dsr.py` (88 total): rejection notifies only on `REJECTED` with the comment included; the create-activity endpoint refuses a student outside the report's batch and routes through the real `create_activity` validation; the history endpoint scopes and orders correctly; full backend suite green (3191 passed, 3 skipped); frontend 117 files / 1075 tests | Adversarial review found **zero findings** — a clean pass, read in full rather than taken at face value given the create-activity endpoint is a new way to create an `Activity` (the exact class of path Phase 9's review caught two bypasses in). Verify itself caught and root-cause-fixed one real issue before Review even ran: `check --deploy --fail-level WARNING` failed because `ActivityDetailSerializer` was imported lazily inside the view method (to sidestep an app-loading-order concern) rather than at module level, so it wasn't available at decoration time for `@extend_schema`'s `responses=` to resolve — drf-spectacular's W002 warning became a hard failure under `--fail-level WARNING`. Fixed by moving the import to the top of the file (verified no circular import) rather than suppressing the warning. `ruff`/`bandit`/migration-check clean throughout | Independently re-read the create-activity view and history view in full given the create-activity endpoint's risk class: confirmed the student is resolved through `students_access.visible_students` (scoped) before the batch-enrollment check, confirmed it calls the real `apps.work.services.create_activity` (not a direct model write) and re-fetches the response through `work_access.visible_activities` before serializing, confirmed the history endpoint gates through the same `_dsr_for` scoping as the detail view. Verified live on staging against a real DSR (616 real reports exist there, unlike the sparse demo-account data earlier phases hit): `GET /dsr/{id}/history/` returned the real `dsr.submitted`→`dsr.approved` chain with actor names and status transitions; `POST /dsr/{id}/create-activity/` correctly refused a student not enrolled in that class's batch with a clear 400 | Did not complete a full success-path live creation (needed a student with a genuinely active enrollment on this specific batch, which took longer to locate than was worth spending on given the negative-path proof plus direct code confirmation of the real `create_activity` call already cover the risk this endpoint introduces); worth noting for future phases that at least one batch (GRS-B-00010) on staging DOES have a real trainer and roster, unlike the mostly-empty demo accounts Phase 12-14 hit — useful for a future end-to-end automation test | 2026-09-17 |
| 14.16 Trainer workspace: Today's Work gains an activities panel+drawer, new `/teaching/work` personal activity list, `GET /api/v1/dashboard/trainer/` extended with `today_activities[]`/`work: {pending, overdue}` | 16 | DONE | `apps/dashboards/views.py`/`serializers.py` (`TrainerDashboardView` extended — `own_activities` scoped through `apps.work.access.visible_activities(request.user)` filtered to `Q(assigned_to=user) \| Q(created_by=user)`, matching `MeActivitiesView`'s own filter exactly; `work.pending`/`work.overdue` always real integers, never null — a trainer with zero activities is a measured zero, matching this codebase's own "null means no data, 0 means measured-zero" distinction; `is_trainer=False`/no-batches branches unchanged); frontend `app/teaching/today/page.tsx` gains an activities panel opening Phase 9's real `components/work/activity-drawer.tsx` unmodified; new `app/teaching/work/page.tsx` — a trainer-personal activity list calling Phase 9's real `GET /me/activities/` directly (zero-diff against Phase 9's own view/drawer code — no duplicated scoping, no simplified stand-in drawer); `app/dashboard/page.tsx` gains "Pending work"/"Overdue work" tiles linking to `/teaching/work`, rendering a real `0` (not a blank/dash) when the trainer has no outstanding work | Extended dashboard test coverage (a dedicated cross-trainer test proves a second trainer's activities never leak into the first trainer's counts); full backend suite green (3195 passed, 3 skipped); frontend 118 files / 1087 tests | Adversarial review found **1 major**: the dashboard's `own_activities` was originally filtered to `assigned_to=request.user` only, while its own code comment claimed to mirror `MeActivitiesView`'s filter — which actually also includes `Q(created_by=user)`. A trainer who created but didn't self-assign an activity would be counted by `/me/activities` (and thus by `/teaching/work` and the today panel) but excluded from the dashboard's pending/overdue tile counts, so clicking "Pending work: N" could land on a `/teaching/work` page showing a different total — exactly the "second way to filter my activities that can drift" risk named in the review brief, just landing in the dashboard extension instead of `/teaching/work` itself. Fixed by matching the exact `Q(assigned_to) \| Q(created_by)` expression. Also confirmed by review as **not a bug** (5 nits): the today_activities/work counts don't leak across trainers (a dedicated test proves it); 0 is always a real measured integer, never null, on both backend and frontend (an explicit frontend test asserts the tile renders "0", not a blank state); `/teaching/work` and the today panel both call the real Phase 9 endpoint/drawer with zero duplicated logic. One minor left as a known limitation, not blocking: the new `today_activities` backend field is fully built and tested but the frontend chose to call `/me/activities` directly for its today-panel instead of consuming it, leaving it computed-but-unread — see Known limitations | Independently confirmed the fixed filter matches `MeActivitiesView`'s exact Q-expression by reading both files side by side. Verified live on staging: `GET /api/v1/dashboard/trainer/` (the correct singular-`dashboard` mount — `/api/v1/dashboards/trainer/`, plural, 404s; this app's URL is mounted singular, a note-to-self for future smoke tests) returned `work: {pending: 0, overdue: 0}` for trainer1, exactly matching `GET /api/v1/me/activities/`'s real `count: 0` for the same trainer — the two numbers agree, which is the whole point of the fix; loaded `/teaching/work` as trainer1 and confirmed it renders the full status-filter list and a correct empty state with no console/API errors beyond the normal pre-login probes | The backend's `today_activities` field remains unconsumed by the frontend (see above) — a future phase could either wire it in (saving the today-panel's separate `/me/activities` round trip) or remove the now-dead computation; neither was done here since the review correctly judged it non-blocking (a minor, not a major) and this phase's own scope didn't require picking one. This is the first phase where staging's `/api/v1/dashboard/` (singular) vs `/api/v1/dashboards/` (plural, a *different* mount for `apps.reporting`'s own dashboard endpoints) distinction actually mattered for smoke-testing — worth remembering for Phase 17+'s manager/counsellor dashboard work | 2026-09-17 |
| 14.17 Counsellor workspace: duplicate detection + wizard step, follow-up activities, `GET /api/v1/dashboards/counsellor/` (plural mount, per `API_CONTRACTS.md`), admissions dashboard consolidation | 17 | DONE | `apps/students/views.py` (`StudentDuplicatesView` — exact email/phone match only, never fuzzy, scoped through `access.visible_students`, the same disclosure-safe queryset Phase 11's global search and Student 360 use — a match outside the caller's reach is never returned, not even redacted; gated on `student.create`, the same capability the wizard itself already requires; `StudentFollowUpView` — a thin wrapper mirroring Phase 15's `DSRCreateActivityView` exactly, routes through the real `apps.work.services.create_activity`, deliberately omits a `channel` field from the request body since the seeded `follow-up-note` form already captures it correctly at completion time, not creation), `apps/students/services.py` (`create_student` gains `override_reason` — **re-runs `find_duplicate_candidates` itself server-side** rather than trusting a client "I checked" flag, and refuses with `400` if a match exists and no reason was given; on override, records `AuditAction.STUDENT_DUPLICATE_OVERRIDDEN` with the reason and matched ids — audit-only, no new `StudentProfile` column, matching the plan row's "no schema" note exactly), `apps/dashboards/views.py` (`CounsellorDashboardView`/`_counsellor_dashboard` — deliberately mounted under the **plural** `/dashboards/` prefix alongside `apps.reporting`'s own dashboards, unlike Phase 16's singular `/dashboard/trainer/`, specifically to match `API_CONTRACTS.md`'s literal `GET /dashboards/counsellor/` spec; every one of the 7 fields built from an existing scoped queryset — `visible_students`, `visible_enrollments`, `visible_batches`, `visible_activities` with Phase 16's exact `Q(assigned_to) \| Q(created_by)` fix reused rather than re-diverged, and the existing `apps.warnings` service's own definitions reused for `unassigned_batch`/`unassigned_trainer` rather than redefined a second time — cached 1 minute per caller, matching `AdminDashboardView`/`ManagerDashboardView`'s house convention); frontend: a duplicate-check step in `app/admissions/new/page.tsx`'s wizard (debounced on email/phone, "Open that record" / "This is a different person" with a required reason held in wizard state until final submit), a "Plan a follow-up" action on the student record, `app/admissions/dashboard/page.tsx` consolidated around the new single-call endpoint while keeping panels the endpoint doesn't cover (e.g. fees) | `backend/tests/test_duplicates.py` (new — exact email/phone match, no match, a match outside the caller's branch returns empty not a stub, override-reason audit written on override and refused without one), `backend/tests/test_student_follow_ups.py` (new — routes through real `create_activity` validation), extended dashboard tests (cross-counsellor isolation, flat query-count); full backend suite green (3224 passed, 3 skipped); frontend 119 files / 1093 tests | Adversarial review, told to scrutinize this phase at the same level as a new authorization surface despite it being framed as UI-integration work, found **zero findings** — a clean pass, verified by reading all four focus areas myself rather than trusting the report: (1) `find_duplicate_candidates` scoped through `visible_students`, confirmed by its own docstring's explicit "false negative is the correct, safe failure mode; a caller learning a student exists that they should not see is not" reasoning; (2) the follow-up endpoint's real `create_activity` call, with a genuinely reasoned deviation from the journey's literal "channel" wording rather than a fabricated field; (3) **the override-with-reason is server-enforced, not client-side-only** — `create_student` itself re-runs the exact same duplicate check against fresh data before ever creating the account, so a client that skips calling the check endpoint entirely still gets refused with `400` the moment a real match exists and no reason was given; (4) every one of the counsellor dashboard's 7 fields reuses an existing scoped queryset, several explicitly cross-referenced against `apps.warnings`' own existing definitions to prevent two disagreeing counts of the same concept. `ruff`/`bandit`/migration-check/`check --deploy` clean throughout | Independently re-read `StudentDuplicatesView`, `find_duplicate_candidates`, `create_student`'s override-enforcement block, `StudentFollowUpView`, and `_counsellor_dashboard` in full given the risk class, confirming each of the four review-focus points by direct code reading, not by trusting the clean review report alone. Verified live on staging: `GET /api/v1/students/duplicates/?email=` found the real matching student by exact email; `GET /api/v1/dashboards/counsellor/` (plural — this endpoint deliberately chose the opposite mount convention from Phase 16's trainer dashboard, and got it right; my own first attempt at the singular URL correctly 404'd) returned rich, real, correctly-scoped data from the actual 616-DSR/1054-enrolment/551-at-risk staging dataset with real deep-linkable warning items; `POST /api/v1/students/{id}/follow-ups/` created a real, correctly-typed `follow-up` Activity end to end, cleaned up afterward via the standard cancel transition | None found — this phase held up cleanly across independent code review and live staging verification with no caveats | 2026-09-17 |

| 14.4 Email OTP: `OneTimeCode` primitive, send/verify services, throttles, step-up's second method | 14 (ERP P4) | DONE | `apps/accounts/otp.py` (`OneTimeCode`, `OtpPurpose`, `send_email_code`, `verify_email_code`), `apps/accounts/emails.py` (`send_otp_code_email`), `apps/accounts/stepup.py` (`step_up_with_email_code`, `require_step_up(code=)`), `apps/accounts/serializers.py` (`StepUpSerializer` accepts `password` XOR `code`), `apps/accounts/views.py` (`StepUpCodeRequestView`, `StepUpView` extended), `apps/accounts/urls.py` (`step-up/request-code/`), `apps/audit/models.py` (`otp.sent`/`otp.verified`/`otp.failed`/`otp.throttled`), `config/settings/base.py` (`otp` throttle scope, 5/min), migrations `accounts/0007`, `audit/0028`; frontend `components/roles/step-up-dialog.tsx` (a mode toggle — password or "Send me a code instead" — D-136), `lib/roles.ts` (`stepUpWithCode`, `requestStepUpCode`) | `tests/test_otp.py` (13): happy path, wrong code, password-XOR-code, unknown-field rejection, expiry, 5-wrong-attempts-voids-the-code (a 6th correct guess still fails), resend-too-soon (service and endpoint), 5/user/hour and 20/IP/hour caps, a genuine two-thread concurrent-verify test (`TransactionTestCase`-style, real connections) asserting exactly one success, and that no `AuditLog.context` ever carries the code; full backend suite unchanged (2810 passed, 3 skipped); `tests/unit/step-up-dialog.test.tsx` (10): password/code toggle, code request and resend, rate-limit sentence, wrong-code error, reset on reopen — full frontend suite 930 passed | 6-digit code from `secrets.randbelow`, stored as `SHA-256(salt + code)` with a per-row salt from `secrets.token_hex`, never persisted or logged in the clear; verification under `select_for_update()` so two simultaneous attempts cannot both succeed; 10-minute expiry, void after 5 attempts, 60s resend cooldown, 5/user/hour and 20/IP/hour caps answered as DRF's own `Throttled` (429 `rate_limited`); delivery bypasses the notification outbox entirely and is sent inline through `apps.accounts.emails`, extending D-070's reasoning (the code *is* the credential) rather than D-069/D-070's exact due-date-reminder path; every outcome audited with the purpose and, for a failure, a reason string — never the code, checked by a dedicated test | Requested a code via `POST step-up/request-code/`, read it from the outbox, verified via `POST step-up/` with `{code}`, got `204` and a fresh `step_up_at`; a wrong code answered `403 step_up_required` and left the session unstepped; two threads racing `verify_email_code` on the same code left exactly one `True` and the row `consumed_at` set, `attempts` at 0. On staging: request-code returned 202, a wrong code returned 403, password+code together and neither returned 400, and the audit trail showed only `{'purpose': 'step_up'}` / `{'reason': 'wrong_code', 'purpose': 'step_up'}` — never the code | This phase's scope explicitly excludes Phase 5's pending-MFA login flow — `OtpPurpose.LOGIN`/`ENROL` are reserved choices with no handling behind them, for Phase 5 and a later enrolment flow to fill in without a migration; real SMTP delivery still blocked on the pre-existing "no SMTP credentials" gap | 2026-09-15 |

## Cross-cutting gaps carried forward

| Gap | Impact | Owner |
| --- | --- | --- |
| No S3 bucket provisioned | The backend, the privacy rules and the boot guard are built and tested (§14.3); a bucket and credentials are a human step | Engineering |
| No managed Redis | Celery worker and beat run against local Redis and are verified end to end (§14.10); a deployment needs a managed broker | Engineering |
| No real SMTP credentials | Password reset, verification and fee-related mail go nowhere; the outbox holds them | **Human** |
| CI runs on GitHub and is red on `feat/erp-foundation` | The workflow runs now that the repository has a remote; its failures are the pipeline's own environment (see the latest run) and have not been triaged | Engineering |
| Staging is on EC2 with a self-signed certificate | Reachable by hostname on :8443 only; a domain or a hosted certificate is needed before a client sees it without a browser warning | **Human** |
| Real trainer and student emails for the SITP batches | 14 placeholder trainer accounts and the students without a sheet email are on the `.invalid` domain | **Human** |
