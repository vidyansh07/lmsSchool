from django.apps import AppConfig


class RequirementsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.requirements"
    verbose_name = "Trainer requirements"
