# Local development and CI. Includes the production stack so the dev image
# behaves like the deployed one (gunicorn, whitenoise) plus test/lint tooling.
-r prod.txt
pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
ruff==0.16.6
bandit[toml]==1.9.4
pip-audit==2.10.1

# In-process AWS. Lets the S3 storage backend be tested for real — upload,
# download, signed URL, object ACL — with no account and no credential in the
# repository.
moto[s3]==5.2.3
