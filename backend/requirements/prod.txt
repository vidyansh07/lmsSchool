# Production / staging runtime (server, static files, error reporting).
# The Redis client lives in base.txt: every environment now sets CACHE_URL.
-r base.txt
gunicorn==26.2.0
whitenoise==6.12.0
sentry-sdk==2.68.1
